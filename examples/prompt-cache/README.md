# Prompt Cache Example (legacy)

This directory is a migration fixture for the retired TypeScript playbook and CLI. Its mock server and `mission.playbook.yaml` are not a supported current workflow.

The current API exposes prompt-cache capability and usage fields through provider configuration, durable provider records, and observability. Consult the generated schema and [provider capability matrix](../../docs/reference/MATRICES.md), then validate any new `agentctl.dev/v1alpha1` workflow with `agentctl check` before running it.
