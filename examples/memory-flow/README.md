# Memory Flow (legacy)

This directory is a migration fixture for the retired TypeScript playbook format. The current Rust CLI does not accept `mission.playbook.yaml`.

Use [`../v1/working-memory.yaml`](../v1/working-memory.yaml) and [`../v1/long-term-memory.yaml`](../v1/long-term-memory.yaml) for supported examples. See [Memory](../../docs/MEMORY.md) for checkpoint, retention, and recovery behavior.
