# Prompt File Variables (legacy)

This directory is a migration fixture for the retired TypeScript playbook format. The current Rust CLI does not accept `mission.playbook.yaml` or its prompt placeholder syntax.

Use [`../v1/dataflow.yaml`](../v1/dataflow.yaml) for supported template and task-output examples. The [YAML reference](../../docs/reference/YAML.md) documents current expressions, variables, and instruction files.
