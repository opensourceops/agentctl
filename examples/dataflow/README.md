# Dataflow Example (legacy)

This directory is a migration fixture for the retired TypeScript playbook format. The current Rust CLI does not accept `mission.playbook.yaml`.

Use [`../v1/dataflow.yaml`](../v1/dataflow.yaml) for the supported
`agentctl.dev/v1` example of scalar and structured task-output propagation,
templates, dependencies, and deterministic assertions.
