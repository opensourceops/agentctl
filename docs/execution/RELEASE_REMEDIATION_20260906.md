# Release containers and remediation execution ledger

## Scope and authority

Implement the approved release/container/remediation task from 6 September 2026. Prepare the internal package release 0.4.0, complete native packages and minimal/tooling images, a standalone Trivy-to-two-agent remediation demonstration, and evergreen documentation. Implementation branches and paired draft PRs are authorized. Creating and initializing only `Ompragash/agentctl-remediation-demo` and generating its labeled remediation draft PRs are authorized. No implementation PR merges, release tags, final releases, production registry tags, production deployments or organization security changes.

The original dirty `/Users/ompragash/agentctl` checkout remains untouched. Its branch is `codex/v0.3.0-release`, source `2aeaa88fba71162206b5f08f5bda4f0150247e4f`, with the original CLI/reference/ledger edits and pnpm cache preserved. The clean framework and docs clones were switched to main, pulled with `--ff-only`, then branched for this work:

| Repository | Fresh main | Implementation branch |
| --- | --- | --- |
| opensourceops/agentctl | cddf1cd0634616cca34f6a7a43acf19c500631c7 | codex/release-containers-remediation |
| opensourceops/opensourceops.github.io | d64d62b7e4277d8e7e48203900697a8397298ccb | codex/release-remediation-docs |

Baseline exact-main CI34035910486, container34035910534, security34035910517 and paired Pages34035955453 passed. They establish the starting state only. The new code needs new execution evidence.

## Decisions before implementation

1. Release sequence: an explicit reviewed source (or operator-created exact tag) starts preparation; four native CLI package targets and both Linux image architectures pass full gates. Every binary, checksum, SBOM, provenance record and verified OCI image archive is attached to a draft release before the operator publishes. Publication verifies the immutable release/source/assets and promotes those same image digests without rebuilding. A validation mode uses a disposable registry and never writes production tags. A published release without complete preparation fails with an actionable instruction.
2. Preserve Linux x86_64, macOS ARM64 and Windows x86_64 packages; add native Linux ARM64. Images use native Linux runners for amd64 and arm64. A minimal final/default Containerfile target retains its explicit agentctl entrypoint, CA roots, nonroot user and read-only-root contract. A tooling target adds the explicitly documented shell/Python/Git prerequisites; models get no Docker socket or GitHub publishing credential.
3. Images/archives, not mutable tags, carry identity between gates. OCI provenance-only descriptors are distinguished from the two runnable platforms. Immutable patch tags are checked before writes; stable aliases move only after all required variant/platform gates and promotions succeed. Reruns reconcile existing digests, and all publisher executions serialize.
4. One ecosystem and direct dependency in the initial useful demo: Python urllib3, with supported fixes chosen only from actual upstream advisory/package and Trivy evidence. The before/after scan must use the same captured database snapshot. No lower-count-only success assertion.
5. One agentctl run connects model-backed analysis and implementation through a validated typed plan, followed by trusted outer tests/build/rescan and a separate credential-free eligibility workflow. Eligibility binds source tree, scoped diff, image identities and reports. The publisher operates in a fresh trusted job, verifies those bindings and reconciles a deterministic branch/PR identity. Outer build/publish operations are not misrepresented as agentctl-ledger effects.
6. Candidate mode requires a reviewed framework SHA recorded in the trusted demo source; an arbitrary dispatch input cannot select credential-bearing runtime code. The normal consumer mode uses the configured published image digest only after it exists.
7. Existing `agents.<name>.reasoning.effort: high` and OpenAI Responses tool/schema support already satisfy Astra configuration. No new model syntax or unrestricted coding tool is warranted.
8. Preserve the original paid SQLite ledger and its uncertain reservation. Reserve a task envelope within its remaining allowance, then allocate durable job leases: at most 30 provider requests, 40,000 total tokens, 900 seconds and USD2 estimated spend. Reasoning tokens are included in output tokens once. Every uncertain dispatch remains charged. No provider calls have occurred for this task yet.

## Environment and configuration

Local runtime OPENAI_API_KEY is present; values were not printed. Local GH_TOKEN and DOCKERHUB_TOKEN are absent. Maintainer GitHub authentication belongs to Ompragash and has access to the two framework/docs repositories. The public demo repository was absent and has now been created at https://github.com/Ompragash/agentctl-remediation-demo; it is awaiting the reviewed bootstrap content.

The demo currently has no Actions secrets or variables. The user has been asked to configure OPENAI_API_KEY and repository-scoped GH_TOKEN (Contents and Pull requests read/write), plus AGENTCTL_MODEL=gpt-6-astra. Values will not be copied from another repository or from the local runtime. AGENTCTL_IMAGE remains a post-release digest configuration; candidate mode avoids depending on an unpublished image. Docker Hub username/token configuration belongs to the later operator release handoff.

The existing Podman Linux ARM64 engine works. Its first sandboxed socket probe failed with an access error; the authorized socket probe succeeded (Podman5.8.2). This is not an engine failure. The earlier Mac loader issue remains historical evidence; use functioning isolated Linux/hosted runners where it persists, and do not relabel an unexecuted local gate as passing.

## Work ownership and current checkpoint

- Primary: release metadata, container definitions, release workflows/scripts, source/package/registry verification, GitHub bootstrap/integration and final reports.
- Docs reviewer: maintained public source prose, docs import/navigation/template cleanup and final paired site checks. Historical evidence remains preserved.
- Example reviewer: new complete `examples/devops/21-container-remediation/` package and its deterministic/live/publisher contracts. Existing shared catalog and tests remain primary-owned integration paths.
- Contract reviewer: new release live-budget coordinator/tests, reusing existing ledger semantics; no real reservations or paid calls during unit tests.

New raw evidence belongs under `.release-evidence/release-20260906/`; prior review and launch artifacts remain source-labeled history. Official GitHub immutable-release/event, Docker multi-platform/token, Trivy database, and OpenAI Astra/Responses/pricing documentation has been read. Exact action and image pins are being resolved from their official upstreams.

Remaining gates: implementation; focused contracts; actual application before/after builds and scans; native image execution on both architectures; disposable registry push/manifest/pull; complete platform/package/security gates; bounded actual Astra tool/structured-output demonstration; hosted demo-created draft PR and rerun reconciliation; paired site build/browser/search/download checks. Production publication and its final smoke test remain pending the user's release action.

Next commands: finish public digest/pin capture; implement Containerfile variants/version metadata and release bundle validators; integrate the standalone example and budget lease contract; bootstrap demo main only after credential-free content review; run focused checks before paid or full hosted validation.

## Implementation checkpoint: native development images and real scan qualification

The original main checkouts were preserved; all work continues in the isolated task clones. The framework task branch now prepares workspace/package metadata for `0.4.0`, adds native Linux ARM64 release packaging, retains the minimal final image target, and adds a narrowly provisioned tooling target. Public dependencies and action references were resolved to reviewed image digests/full action commits in `scripts/release/inputs.json`.

The new release implementation uses four native package jobs and four native image jobs, exact OCI transport, blocking image scans, CycloneDX SBOMs, native offline replay, disposable index promotion/pull/rerun checks, and a GitHub-attested complete bundle. Draft attachment precedes publication and requires an operator-created matching tag; release publication verifies prepared assets and promotes without rebuilding. Stable tags share one concurrency group. Production Docker Hub writes, release/tag creation and new PR merges have not been performed.

A shared live-budget coordinator reserves one task envelope in the pre-existing original ledger and issues run-bound CI leases. It retains the original envelope whenever any receipt/effect is uncertain. Its tooling installation includes exactly the coordinator, existing live-command wrapper and existing shared ledger implementation. No real task reservation or paid call has been made at this checkpoint.

Preliminary development evidence (dirty source, revision label `42f27fd`; **not final-source release evidence**):

- `cargo check --workspace --locked` passed in native Linux ARM64 after scoped CA/cache prerequisites were resolved.
- `cargo fmt --all` and `cargo xtask generate` completed; generated CLI/schema references reflect the changed package metadata.
- Minimal image `sha256:04ebbda8366f48ad80538569b3c1f5e3bef7739232bdabee48c5ad031ad61640` and tooling image `sha256:efafef5df678e9e83c93e72a84212caa36aa988ff0e64ed946e0b79d8a3ac2b3` both ran version/check/plan/run/inspect/replay on native ARM64 with a read-only root and non-root mounted-directory ownership.
- Removing mutable defaults before replay preserved recorded output/artifact hashes and produced zero effects/provider sessions/tool calls. Minimal rejected shell/Python/Git; tooling exercised its actual shell, Git, packaging and YAML dependencies. The old separate `/config` mount failed before dispatch with source-validation exit `2`.
- Forty release artifact/bundle/promotion regressions passed, including stale provenance, omitted/mismatched platform evidence, invalid scan/SBOM/smoke records, immutable tag conflicts, partial pushes and uncertain acknowledgement reconciliation. Release workflow actionlint passed locally; hosted execution remains required.
- The isolated demo before image `sha256:0ce04b7091645be5fa37c4baca9b4da7ebbedb65729869771286dacc74f4306e` produced actual Trivy findings for urllib3 `2.6.2`: HIGH `CVE-2026-21441`, `CVE-2026-44431` and `CVE-2026-44432`.
- A **trusted fixed fixture**, not model-generated remediation, built image `sha256:51273d39bfec840c39ae72c4b3f5a310aeffc90e9380cc5fa030cf31c135625e` with the reviewed hash-locked `2.7.0` wheel. Its two application tests passed. Real rescan removed all three target findings and introduced no new findings. Before/after scans used the exact same read-only Trivy DB/metadata hashes; database bytes were checked unchanged.
- Ordinary cookbook validation now retains all twenty catalog entries and separately inventories example 21 and its credential-free contract tests. It installs packaging tooling, never the deliberately vulnerable application dependency.

The public demo repository now has the **names** `OPENAI_API_KEY` and `GH_TOKEN` in Actions secrets and `AGENTCTL_MODEL` in Actions variables. Values were not retrieved. Initial reviewed application/workflow bootstrap, exact-source hosted builds, the bounded Astra run and real draft remediation PR still remain.

Recorded failed attempts are retained under the local release evidence directory: missing offline Rust dev dependency; mismatched existing cache UID; initial distroless registry CA trust; missing tooling build-context include; too-small Trivy temporary storage; Trivy cache UID mismatch; unsupported OCI-tar scanner input; and two documentation harness expectations (`outputs` versus actual `data.output`, policy denial versus source-validation exit). These were fixed/retried explicitly. TLS verification and framework policy enforcement remained enabled. None of the failed attempts count as passing evidence.

Next: complete maintained example README/runner recovery integration; commit the integrated source; execute all current credential-free source/package/image gates on that SHA; export/bootstrap the standalone demo; reserve only the necessary paid leases; run hosted candidate remediation and reconciliation; pin/sync/build/verify paired docs; deliver linked draft PRs and final source-specific evidence. The final published-image smoke remains pending the operator's release action.

The first full verification run completed production compilation and Rust tests, then found stale `0.3.0` path-dependency constraints in the separate fuzz manifest. Those three constraints were corrected to match the workspace; the earlier failure remains in `verify-development-wheelhouse.log`. Native test prerequisites use an extended task-local offline wheelhouse containing the newly declared packaging dependency. The new standalone inventory/contracts test passes in that declared environment. A host Python run without packaging failed as a prerequisite error and was not counted as source failure.

A non-billed, TLS-verified OpenAI model metadata GET returned HTTP 200 for `gpt-6-astra`; the demo's non-secret `AGENTCTL_MODEL` value also matches exactly. Tool calling, structured outputs and actual usage remain to be proven with reserved paid leases. Demo config does not imply a successful hosted live journey.

The release tag contract also reserves the `-ci` suffix: a minimal prerelease ending with it would collide with another release's tooling tag. Such versions and SemVer build metadata are rejected before publication. Forty-one release regressions and eighteen coordinator tests pass locally at this checkpoint. Example 21 includes a no-paid reconciliation operation that retrieves exact successful remediation-job evidence and reuses the same source/fix fingerprint, rather than invoking another model run.

## Integrated verification checkpoint

Framework draft PR: https://github.com/opensourceops/agentctl/pull/8. Its initial source `617e1a3a3ef91e40ca2289d86654b1beacaa35a6` passed hosted container run `34049380139` and supply-chain/security run `34049380145`. Credential-free run `34049380194` failed on all three native platforms because the reusable-pack lock still recorded engine `0.3.0`; that lock is now regenerated for `0.4.0`. The separate local cookbook runner also exposed an exact-directory inventory check that omitted the independently cataloged example 21. The corrected union still requires precisely the twenty original IDs and standalone ID 21; execution of the original twenty remains intact.

The new example now has a separate actual Responses preflight operation: Astra/high, `store:false`, one pure echo tool, strict output and recorded tool-result assertions, and keyless/networkless replay. Its dedicated lease permits at most 3 requests, 8,000 total tokens, 120 seconds and USD0.20 estimated cost; the larger token reservation accounts for encrypted continuation context without enabling provider-side storage. These limits are part of, not additional to, the shared task envelope. No paid preflight has occurred yet.

The real CLI in the preliminary tooling OCI image passed the synthetic interruption/repair contract: a terminal assertion failed immediately after successful analysis; selective repair reused `capture` and `analyze`, executed only the scoped implementation work and produced zero new analyzer effects. Replaying the repaired run produced zero fresh effects. Independently failing tests, build, and scanner flags each produced exit 4 and no publication record. This is terminal-failure selective-repair evidence, not a process-crash or same-run-resume claim. Raw report: `example21-recovery-contract-01/report.json` in the local evidence directory. Tooling release jobs now require this contract on each native architecture and bind its image identity and report checksum into the release bundle.

Forty-two release regressions and twenty-six standalone helper/contracts tests pass after integration; the final hosted native/package/OCI gates and actual leased live demonstration remain required. Preliminary docs checks at `617e1a3` passed 94 HTML pages and all twenty-one downloads, with 140 browser checks passing and one existing generic mobile-search matrix skip (dedicated cookbook/remediation mobile search passed). The combined paired gate failed on the same stale framework pack lock, so this is not a passing final paired verdict.

## Standalone bootstrap and hosted preflight checkpoint

The authorized demo default branch is initialized at `9149d9f278f7af399163619a2607d4a7fec7d89a`, exported from clean framework `732fef49fc8cde02e75f8e9d50974fe6cc816e9b`. Release preparation `34050254020` is running without draft attachment or publication. The first demo preflight run `34050403496` failed its credential-free export regression before the credential-bearing job: GitHub reports preflight `skipped` with zero steps. Actual paid provider requests remain zero. The original budget ledger retains its prior uncertain reservations and the new task envelope; the first 3-request/8,000-token lease is being reconciled only against that authoritative skipped-job proof.

The exporter copied an existing standalone `package-manifest.json` and then overwrote it, causing a self-hash mismatch. The fix excludes only the generated root manifest and is covered by a regression using an already exported Git checkout; 14 contract and 13 adapter tests pass there. The full framework inventory also exposed four missing matrix rows for the new package; real CLI check/plan observed exit 0 for all three agentctl workflows and exit 2 for the GitHub Actions YAML. Those rows and the independently leased OpenAI inventory are now recorded explicitly. A private GitHub settings link returned 404 in the public site checker; the guide now links the public repository and gives the settings breadcrumb in prose.

All twenty original cookbook cases and framework `docs-verify` passed locally after the pack-lock/catalog fixes. Two subsequent local attempts exposed runner setup mistakes (an unmapped Mac target symlink and a missing declared Python environment); logs retain these prerequisite failures, and neither counts as passing evidence. The isolated Linux cache and pinned helper environment remain the validation path.

Native release preparation at `732fef4` completed image build, native execution/replay, blocking scans and SBOM creation for all four platform/flavor combinations, then failed its final record validator. BuildKit `v0.32.2` uses the modern OCI empty attestation config (`{}`), whereas the validator expected the older unknown-platform config. The corrected verifier accepts the documented empty descriptor while requiring the non-runnable index platform, exact config identity, matching manifest subject and matching in-toto statement subjects. Modern/legacy round-trip and malformed subject/config/platform cases are included in 44 passing release regressions. The next hosted run must still prove the actual OCI transport/bundle path. Failed image artifacts will now be retained without allowing failed jobs into assembly.

The first preflight's unused lease was reconciled after fresh GitHub exact-attempt verification: proof SHA256 `5d9a4a1991c1f6e8ac10095e484065fd8f93ff6e2367c6037e392723767ddcf4`; all four charged counters are zero. Twenty-five coordinator regressions cover this separate skipped-job proof, wrong identities, started/uncertain jobs, missing inventory, crashes and idempotent reconciliation. The original task envelope and prior uncertain reservations remain intact. All twenty original examples and the expanded strict matrix passed `cargo xtask examples-verify` with the declared Python environment.

The second native preparation (`34051210706`, source `46a8923`) retained actual OCI output and exposed an unnamed-export producer issue: its valid modern attestation manifest had an empty SLSA v1 statement subject list. BuildKit needs an image name to populate that subject. The build now uses a deterministic local `agentctl-release` name while continuing to export OCI only, without a registry push. The checker still rejects the captured empty-subject archive; 45 release regressions cover that actual format and preserve strict subject binding.

Automatic approval review rejected the five-file demo bootstrap update to `main` twice, treating default-branch changes as requiring new explicit approval despite the initial-setup authorization. The safe review alternative is https://github.com/Ompragash/agentctl-remediation-demo/pull/1 at `ec177fd4d9843d003710a41cf67478c2595422e8`. Its hosted contracts pass; no application or dependency bytes changed. Explicit approval to merge that bootstrap PR is pending. The real preflight/remediation jobs require trusted `main` and remain blocked on that approval; their guard has not been weakened. Actual paid usage remains zero.

Full local `cargo xtask verify` passed at `f54ccd6`, after installing pinned cargo-deny `0.20.2` in the disposable Linux tool cache. The separate credential-free CLI acceptance passed all 46 scenarios, and `cargo xtask completeness` passed its packaged durable-recovery, operational compensation/protocol, and typed-handoff compositions. The artifact-store, retained-migration and protocol unit cases are included in the full workspace verification rather than repeated as redundant paid or local runs. Raw logs: `verify-f54ccd6.log` and `acceptance-completeness-f54ccd6.log`.

The paired site at `f54ccd6` passed all content/freshness, 94 route/anchor artifacts, source/download checks and 140 browser checks, with the single existing generic mobile-search matrix skip; dedicated twenty-example and independent remediation mobile tests passed. Its link-check transport was repaired after completed HTTP assertions left sockets open under the local Node runtime; the failure and bounded transport regressions are preserved by the docs repository.

Hosted preparation at `46a8923` passed native Linux AMD64, Linux ARM64 and macOS ARM64 package gates and their package/SBOM checksums. Windows found a test fixture calling Linux-only UID/GID helpers. The fixture now supplies explicit Linux identity and POSIX mount paths, preserving every security assertion; a simulated missing-UID/GID regression and all fourteen contract tests pass. This does not introduce Windows execution support for the Linux-container demo. The final hosted Windows gate remains required on the corrected source.

## Exact-source native and paired documentation evidence

Framework `68e5b8e738f099487c7af9fe1b043ab2c1a5d0b0` passed credential-free CI `34052384873` on Linux, macOS and Windows, including its production SBOM. Supply-chain/security `34052384859` and container/security `34052384864` passed. Native release preparation `34052467088` passed **all four native package jobs and all four native image jobs**. Every native image archive and record was downloaded and checked against its GitHub artifact digest, runnable manifest/config, source labels, nonempty SLSA subjects, scan, platform SBOM and native execution/replay report. Both tooling images also passed their actual denial/recovery contracts.

That preparation is still a **failed complete-release gate**: assembled OCI transport stopped in Skopeo while unpacking an implicit parent directory (`chown .../blobs/sha256: operation not permitted`). Registry index promotion/rerun/pull and complete-bundle attestation did not pass. The merged archive metadata is being corrected without changing image manifests or blobs. Preparation also now verifies its retained Sigstore bundle using the same exact source/signer and hosted-runner policy as draft attachment/publication, so validation mode exercises verification before artifact upload.

Paired docs PR: https://github.com/opensourceops/opensourceops.github.io/pull/7. Tested docs `35d15cd26d693d21f67d9e13992fa96b72784c2c` pins framework `68e5b8e`. Its local and hosted run `34052645754` passed 140 browser checks (one unchanged generic mobile-search skip; dedicated example/remediation mobile checks passed). The actual uploaded artifact `9995138253` has SHA256 `4467a92dbaa8bb9abeaf49472fe6d16a4469c9f0e105a6486cbad9965ec89572`, matching GitHub's digest. Its 94 HTML pages, `.nojekyll`, all twenty original packages plus the independent remediation package, and each package manifest match validated source bytes; visible canonical boilerplate and retired public branding are absent. Deployment was skipped. Docs head `eb88cdd9583c12e88f8d22e6021fef4120e3e650` adds only execution evidence to that tested source.

The pending demo bootstrap PR now has head `d0711894a75a2e0bad08a598cfa89eb63946a198`; its credential-free contracts passed in `34052489853`. All 34 tracked files match the clean export from framework `68e5b8e` byte for byte. Its default branch remains `9149d9f278f7af399163619a2607d4a7fec7d89a` until explicit approval resolves the previously recorded automatic-review block. No paid requests, tokens or estimated spend have accrued; the task envelope remains reserved and the skipped first lease is reconciled to zero.

Read-only qualification of the earlier real application scans now binds both saved Docker archives' config/image IDs, layer hashes, application/test/lockfile bytes, installed urllib3 versions, build instructions and pinned base to the final example inputs. Only the reviewed `2.6.2` to `2.7.0` dependency/hash patch differs. Recomputed findings are 273 before and 270 after: all three target HIGHs removed, zero new findings, and **270 unrelated residual findings retained**. These earlier builds are fixed-fixture evidence, not fresh builds at the final source SHA or model-generated remediation. Evidence: `fixture-source-parity-68e5b8e.json`, SHA256 `6cda81f10201f08ecf60d18cd5bfdd256ccbb3aeabad81c74087aa8cfa4d87b3` in the local evidence directory.

Framework Actions configuration names `DOCKERHUB_TOKEN` and `DOCKERHUB_USERNAME` are present, as are the previously verified demo secret names and model variable. Their existence does not prove publisher permissions; no production Docker Hub operation or actual demo-token publication has occurred. Next: prove the corrected archive with actual unprivileged Skopeo and a disposable registry, commit the surgical fix, run complete release preparation on its source, repin/revalidate paired docs and the pending demo export, then continue the bounded live jobs only after bootstrap approval. Tag creation, draft attachment, final publication and the published-image smoke remain separate operator actions.

The archive correction now passes an actual local end-to-end transport check using the retained `68e5b8e` native artifacts. Explicit `blobs/` and `blobs/sha256/` directory entries precede files; manifests/blobs and all index/platform digests remain unchanged. Nonroot Skopeo `1.22.0` reproduced the original ownership failure, then imported the corrected archive. Complete bundle verification passed; first promotion created six exact tags, rerun reused all six, and four platform pulls matched the original image config IDs. The loopback registry was cleaned up. Local engine adapters were restricted to the exact disposable HTTP endpoint and Podman's image-ID format; production scripts and digest assertions were unchanged. The initially inherited root HOME/auth path was a retained local harness failure before a registry write. Evidence: `native-release-34052467088/transport-reproduction/{commands.json,first-promotion.json,rerun-promotion.json,registry-smoke.json}`. All 46 release regressions pass. A new exact-source hosted run must still validate the integrated fix and GitHub attestation.

## Corrected archive and Docker platform-store checkpoint

Framework `ce00ab9ad600bbe6f95e75ef8cf4d800efc67bba` passed all eight native release jobs in `34054932901`, plus CI `34054923020`, security `34054922989` and container `34054922920`. The first bundle attempt correctly stopped because the independently running Windows CI prerequisite had not finished. After CI passed, only the failed bundle job was rerun; the successful native artifacts were reused. Attempt 2 passed real unprivileged OCI import, first promotion and rerun reconciliation, then exposed a Docker classic image-store restriction: pulling another architecture through the same local index reference fails with `cannot overwrite digest`. No signing step ran. The platform probe is being corrected to remove only its own pulled local reference between probes, while preserving remote digest and actual image-ID assertions. The earlier Podman platform pulls were valid Podman evidence and did not establish this Docker classic-store behavior; an actual disposable Docker probe is being investigated before the next hosted run.

Paired docs at `4d527f9050e6932301b31b413fc9dbd4b29de112` pins framework `ce00ab9`. Local and hosted `34055218810` passed the full pair, 140 browser checks (one unchanged generic skip), 94 pages and all 21 packages. Actual site artifact `9995863202` has SHA256 `78bd1eb18341279255a7f43999b761529eace1bde3341c1ace2de2c53b2a9895`; its standalone remediation ZIP is byte-identical to the clean framework export, SHA256 `9536c86df930681f18e9aa7e6f6a98323742f73db48722810029aca665e9fce9`, manifest SHA256 `513c29c7e7ab441cf129c52e122c77cdf8610a8da537ffa2f7cc33433a1aaff8`. All 34 tracked files in demo PR head `b61cca7f19ec8d1e0416f175658fe9e5f0b164aa` match that export; its credential-free run `34055033424` passed. Docs head `14ff06d9fa148cafd6d5497facab6ea4a3be05cb` adds execution evidence only. The main-branch approval block and zero new paid usage remain unchanged.

The platform-probe correction now passes actual **Docker 28.5.2 with classic vfs storage**. An isolated digest-pinned Docker daemon reproduced the original same-index AMD64/ARM64 failure, then the corrected script pulled and inspected all four minimal/tooling platform configs. It removes only each successful probe's exact local reference; both remote index digests remained unchanged. The daemon used private storage and an internal network, no privileged mode, no host mounts/socket or credentials, and only the required additional SYS_ADMIN/MKNOD capabilities inside the existing isolated Podman VM. Task containers, stores and network were removed afterward. Evidence: `docker-classic-probe/report.json`, SHA256 `afb6df139d91d584622f465616113cb3c09df3af8060fab3710ba115641ef689`; tested script SHA256 `6328e73307def0ffbc4b340738f2cdd30e68b02a3e0efbc14ab10b71420d4865`. The deterministic lifecycle regression also fails against the pre-fix script and passes after correction. All 49 release tests pass, including wrong-config denial and failed-pull ownership cases. The next exact-source hosted run must still complete the signed bundle; all prior failure/partial-success evidence remains retained.

## Final native validation and budget-test ordering correction

Source `b91dc4dbc8300bc12809d9d667e07a5ae7465f8f` passed all eight native jobs in release preparation `34056888292`. Independently retained package/image evidence is `native-release-34056888292/native-validation.json`, SHA256 `516248b94ae5364dfe99dfc30ed88870b4ad30677ea759e46549975b8aa37837`. Supply-chain `34056858852` and container/security `34056858846` passed. CI `34056858871` failed only a Windows budget-test assertion that treated the last row ordered by timestamp/UUID as the newly allocated envelope. Equal creation timestamps can place the older uncertain reservation last; production accounting already selects reservation IDs. Bundle assembly correctly stopped on this failed CI prerequisite before registry/signature steps. Both failure logs are retained.

The five affected original-ledger test assertions now identify the exact old reservation or leased envelope. The skipped-job regression explicitly covers tied timestamps and both temporal orderings, preserves the full task envelope, preserves prior uncertainty, and verifies close releases only the unused task envelope. Restoring the old positional assertion fails this regression; all 25 coordinator tests pass with the correction. No production code, budget limits or reconciliation policies changed. The corrected source still requires exact-source hosted CI and complete signed-bundle validation. Paired documentation run `34057208999` passed for docs `133e8f2f3bd570b6f9c454466ddf9f806ff42b14` / framework `b91dc4d`; its retained artifact evidence remains valid for that pair. Paid usage remains zero, and the demo bootstrap default-branch update remains pending explicit approval.

## Final validated release and documentation checkpoint

Code under test `d3f4338a7735ff610947c1232ebf7797f584993d` passed CI `34091947786`, supply-chain/security `34091947756`, container/security `34091947743` and complete release preparation `34091976162` attempt 1. All four native packages and four native images passed. The complete bundle passed actual disposable-registry promotion, six-tag rerun reuse and all four Docker platform/config pulls. Ten downloaded artifact ZIPs matched GitHub hashes, both complete OCI indexes and all 35 checksum-listed files verified, and an independent exact-source/signer GitHub attestation check passed with self-hosted runners denied. Verification report SHA256: `b25bac124f40e77aa2dec8d2d38e7a411bca5a314d1534be4d39faee3b52021f`. A transient GitHub metadata-read EOF was retried successfully; it did not rerun a workflow or change evidence. Draft attachment was intentionally skipped (`attach_draft=false`).

Docs code under test `87ff6cf135fe5a1cec6efddbc92953439e1ae660` passed local and hosted paired validation `34092386026` against that exact framework source: 140 browser checks, one unchanged generic skip, 94 pages and all 21 packages. Docs head `669f265b6331195a5c0f43aed8307a4019d2642c` adds evidence only. Actual uploaded site ZIP SHA256 `dccf9777467c25bcb5e4804efb67a8eccf14119527d1825e037a065ac75fcc31` matches its Actions digest. The nested remediation ZIP matches the clean export and all 34 tracked files in demo bootstrap PR head `59104af1f9fb16bb31a806a35af1508251066bc1`; its contract run `34092089701` passed.

[Final maintainer handoff](RELEASE_REMEDIATION_HANDOFF_20260907.md) records exact binary/index/platform digests, release sequence, settings and remaining gates. This checkpoint and handoff are evidence-only additions after the tested framework commit. Production tag/release/Docker Hub/Pages actions remain unperformed. The automatic-review block on the five-file demo bootstrap default-branch correction still requires explicit merge approval; no paid model request, model-generated remediation PR or live PR-rerun proof is claimed. Keep the task budget envelope reserved, with zero charged usage and prior uncertainty preserved. After approval, merge only demo PR1 after checks, derive the actual new main/run identity, reserve a new preflight lease, execute/reconcile it, separately lease the full remediation, and validate the no-paid reconciliation path.


## Authorized publication follow-up — 8 September 2026

The user authorized all four remaining actions. Framework PR8 merged as `a1578da9c3a6d38052798f93384d2fa5278d857f`; demo bootstrap PR1 had already merged as `223e3093cae7bbb02715d5f1725d3c0f4b000724`. Automatic approval review separately blocked docs PR7 because its merge triggers public Pages deployment; the explicit deployment question remains pending. No Pages deployment, GitHub release publication or Docker Hub promotion has occurred.

Real candidate preflight `34155607816` passed on that demo main with framework `d3f4338a7735ff610947c1232ebf7797f584993d`, actual `gpt-6-astra` / high reasoning / `store: false`, one exact echo tool call and strict output. Independent SQLite/artifact inspection proved keyless networkless replay had zero fresh effects/provider requests. Its reconciled receipt records 2 requests, 275 tokens, 6 seconds and USD0.003990 estimated. Artifact SHA256: `23828bc90467dda44109f5bcdd8824e7bcf0ae406cb1dd3d01f20bbff24d4f6c`.

Full remediation `34156165704` passed trusted preparation, real analysis and deterministic plan validation, then failed before either write tool: OpenAI HTTP400 rejected newline-containing string enums in strict `write_manifest` input schema (request `req_09d126e02c2f4719a142a4582624920f`). No model patch or remediation PR was produced. The failed terminal run has no outstanding reservation or uncertain effect. Its retained artifact SHA256 `e910f1183e019efb08ecf93efdc8e4d309952eefe83f61fa140d6b124093e7ff` and durable SQLite usage match the reconciled receipt: 2 requests, 2,269 tokens, 24 seconds and USD0.065810 runtime estimate. Current published cache-write pricing adds USD0.002970 omitted by that workflow's estimate; the original receipt remains unchanged. Combined runtime receipts total 4 requests, 2,544 tokens, 30 seconds, USD0.069800; corrected published-rate estimate is USD0.072770, not an invoice. Original uncertainty and task allowance remain preserved.

The first tagged preparation `34155764812` at `v0.4.0` / `d3f4338` passed all eight native jobs and the signed bundle but failed draft attachment. After creating an unpublished draft, the script queried the release-by-tag endpoint and received404. No assets were attached by that failed job. Publication is held while the exact-content schema, cache pricing and first-draft lookup defects are corrected with regression coverage. The task-created tag and empty unpublished draft are superseded preparation state; their old source and raw evidence remain recorded, and no immutable published version may be replaced.

Next: validate the authority-preserving schema through actual CLI denial fixtures; test first-draft discovery and published-release rejection; review/commit the correction, synchronize the demo and docs packages, run bounded live remediation and no-paid reconciliation, prepare the corrected exact source, verify complete draft assets before publication, verify Docker Hub digests and published consumer smoke. Docs merge still requires the pending public Pages approval.

Focused validation passed:13 adapter tests,16 package/contract tests, and the actual Podman CLI contract including19 rejected writes (`schema-contract-20260908/report.json`), exact positive writes, selective repair and zero-effect replay. A host-wide `test_*.py` invocation incorrectly included the application-only test and failed importing `app`; the documented helper-only test commands then passed. The deliberately vulnerable application dependencies were not installed into the host; its unchanged tests belong to the isolated trusted image build.

The draft lookup correction passed56 release tests, including seven new regressions. The first-create regression fails the old source with the observed404 and passes the authenticated paginated lookup. Existing-draft byte reuse, changed-asset rejection, missing/ambiguous lookup, already-published/prerelease mismatch and source/signature denial remain enforced. Source fixes are ready for new exact-source hosted validation; provider regex acceptance and complete corrected draft attachment are still unproven.


The next live attempt `34158074861` (demo main `91c691c6859347793c0c9b61727a1ad783d076db`, framework `42922f9479f9b9cda360c36cf9b113ae4ce50f9a`) also stopped before writes: OpenAI HTTP400 explicitly rejected regex lookaround, request `req_2dc6deda52f94f889ec49f0ff3a43088`. The service documents `pattern` but not this dialect restriction. The earlier local runtime/ECMA acceptance did not establish provider acceptance. Its receipt remains reserved until the complete artifact and durable SQLite state are verified.

The replacement uses the documented simple `^literal$` syntax. In agentctl's pinned Rust JSON Schema validator, default `$` is absolute end; the actual CLI rejected all19 malformed content/path cases, including an additional newline, and accepted both exact reviewed contents. Python/JavaScript soft-end search semantics are not claimed as runtime parity. The isolated actual CLI report is `schema-dollar-evidence/report.json`, including compatible selective repair and zero-effect replay. An initial scratch run placed output under its source tree and failed recursive copying; task-created scratch output was removed and the test was rerun with a separate output directory, without modifying production copy logic. The preflight is being strengthened to exercise both real multiline write-input schemas through a pure echo before another analysis request.

The full run10 artifact SHA256 `567fc20be4cf8d655fc44ef98fa5ec5df847cdc812219703cc4f2a1c71e4f6f7` now matches GitHub metadata; its original lease, durable failed state, zero writes, known effect statuses and empty runtime reservations match the receipt. Reconciled usage:2 requests,2,314 tokens,28 seconds,USD0.070805 with cache-write pricing included. Across this task the three live attempts total6 requests,4,858 tokens,58 seconds,USD0.140605 runtime estimates (USD0.143575 after the separately recorded old-price correction). No uncertain charge was erased or allowance reset.

The strengthened preflight now uses a typed manifest/lock prompt and echoes both real input schemas without write authority. Its17 focused tests passed. Real CLI verification succeeded with the exact nested input/result and zero-effect replay (`schema-preflight-typed-20260908/verified-report.json`). Immediate first reads through the local Podman VM mount reported truncated YAML; independently reading the unchanged mounted4162-byte file (SHA256 `64322c414b1b50c386c2da5853779e7363656d636825ddcc165303bfc2b34bdd`) and read-only compilation succeeded, then the same file ran and replayed successfully. No parser, YAML-writer or production runtime workaround was introduced; hosted native execution remains the final source gate. Historical429 preparation34157791134 passed all8 native jobs and bundle assembly, but that source still has the now-rejected lookaround; it is not publication evidence for this correction.


## Exact-length provider compatibility correction

Framework PR9 merged as `ac6cc5e749b8394de30218a7fceadee8b6086ea5`; tested source `ed604369fb73a3d1bba65d8a2026927f59b14e97` passed CI34159477243, container34159477213 and security34159477224. Demo maintenance PR3 merged as `3345f169eaaf6934ff4940e025471fe22a371517`. These maintenance PRs are not model-generated remediation evidence.

Stronger live preflights34159738279 and34160634920 accepted the plain anchored schemas, then both returned the same65-byte prefix of the158-byte lockfile. The persisted full prompt and tool schemas match authored inputs; runtime rejected the response before the pure echo dispatched. Neither attempt proves successful structured output or replay. Normalized finish reason does not preserve enough raw response status to establish an output-limit cause. Their independently verified complete receipts are reconciled: together2 requests,934 tokens,11 seconds,USD0.015660 estimated. Task totals are8 requests,5,792 tokens,69 seconds,USD0.156265 runtime estimates, or USD0.159235 including the historical USD0.002970 cache-write correction. No uncertainty or allowance was reset.

The next surgical correction adds `minLength` and `maxLength` equal to the reviewed content lengths (104 manifest,158 lockfile), retaining both exact anchored patterns, unchanged bytes and unchanged permissions. Seventeen contract and13 adapter tests pass; new assertions fail against the preceding schema. Current official Structured Outputs documentation lists these length keywords as additionally unsupported for fine-tuned models; base Astra compatibility still requires a fresh bounded live probe. No unchanged semantic retry is planned.

Automatic approval review separately rejected replacing the task-created unpublished v0.4.0 tag (old source d3f4338) with corrected source. No part of that rejected command executed. The old tag and empty unpublished draft remain untouched; explicit tag-correction/publication approval is pending, alongside the earlier public Pages merge approval. Native release preparation34161018607 is nonpublishing evidence for ed60436 only; a later source needs its own final gates.


The exact-length local OCI contract passed66 credential-free/networkless CLI invocations, including19 denied inputs with zero write effects, exact104/158-character echo, strict output, zero-effect replay, selective repair and three denied publication gates. Report SHA256 `0bef29d2b70245fc5b5a7af4e245e037197bdbb01d579dfa5ab3265ed5ee52c1`. Its first attempt reproduced the local shared-mount EOF at exactly the previous file length (3909 of4286 bytes); the identical stable file subsequently passed. The test fixture now writes its synthetic workflow to `scripted-preflight.yaml`, preserving the previously inspected authored file. This is a fixture portability correction; provider requests, production YAML and runtime behavior are unchanged. Retained development image efafef5 was used, so final-source native image validation remains separate.

Demo maintenance PR4 passed contracts34161870186 and merged as `781f373302b5dee662a8363b577a982d22a0c3fb`. Fresh live preflight34161943315 uses framework458ba25 and a new3-request/8,000-token/120-second/USD0.20 lease; its outcome is pending. The subsequent fixture-only source correction does not alter that live probe's provider input.


Live15 (`34161943315`) accepted the exact-length schemas but ended `max_tokens` with no tool call or visible output at the512-token output ceiling. Artifact SHA256 `87c7dd7d1a7a0a50b6ca5c3dc1d2c82b130f2a6347709f1895b071be62ee09b8`; verified report SHA256 `805885d5b4a681090161c66ec5803e26c11b28f2014dae874d602fa0b195d916`. Its normalized response has zero token counters and no retained raw usage-presence evidence. The original receipt is retained unchanged but deliberately NOT reconciled: the entire3-request/8,000-token/120-second/USD0.20 lease remains reserved. Zero billed usage is not established.

The next preflight allows2048 output tokens, retaining its existing3-request/8,000-total-token/USD0.20 caps and exact-content permissions. Official reasoning guidance confirms output limits include hidden reasoning and can stop before visible output. Seventeen focused tests pass. Separately auditing missing-usage handling is required before treating budget accounting as release-ready. The queued964 preparation34162278914 was cancelled before native jobs started because source needed this live-discovered correction; it is not passing evidence. No unchanged paid retry was issued.


The missing-usage correction now rejects absent/null/noninteger/negative/out-of-range required OpenAI counters before output acceptance, marks a dedicated `UsageUnavailable` error uncertain and leaves its original reservation active. Explicit zero and complete known usage remain valid. Fourteen focused OpenAI adapter tests and two runtime accounting/recovery tests pass;26 shared-budget coordinator tests pass. The runtime regression demonstrates retained request/token/cost reservations, no automatic repeat or same-run-resume dispatch, no write effect, and a separately budgeted explicit terminal retry that preserves the original uncertainty. An initial test expected replay despite a pending downstream task and failed; its assertion was corrected to the existing refusal/no-new-call contract, without changing production replay behavior. Independent review found no new blocking issue in this narrow diff. Five pre-existing OpenAI success fixtures now include explicit valid usage so their original continuation/tool/error assertions remain meaningful. Actual live15 usage is still unknown; the defensive parser regression does not retroactively establish what the remote raw response contained.

Full provider tests32 passed, including shared-adapter and transport cases; workspace/all-target Clippy with warnings denied passed. The two focused runtime accounting cases and26 coordinator cases passed. The source is ready for final hosted checks and a newly leased2048-output-token preflight, with the prior uncertain lease still fully reserved.


Preflight18 (`34163342194`, frameworkc223d01/demo bdba15bfdeac13b0f4a0b84a6dbcb7a2786f7a59) returned an empty incomplete response with explicit numeric zero required usage counters. Its exact-source parser proves presence of those counters, so the original verified receipt was reconciled as provider-reported1request/0tokens/4seconds/USD0, not an invoice assertion. Artifact SHA256 `a55d22115e429cb6b1ec11456cc32a27c056e7724e4d081fc3a04e667eee1b5d`; receipt SHA256 `48034019873e43121e316a0ea00afd2f75e88bc6a69a5e48ee2559a32efb50b2`. Known reconciled totals:9requests,5792tokens,73seconds,USD0.156265 (USD0.159235 after the old pricing correction), plus the unchanged full unknown15lease.

Correction to earlier interpretation: the adapter mapped every `status: incomplete` response to `max_tokens` and discarded `incomplete_details`; therefore neither15 nor18 proves output-cap exhaustion. The2048cap increase was based on that misleading diagnostic. No further limit increase or unchanged retry is planned. A narrow classification correction will distinguish explicit max-output-token exhaustion, content filtering, and an unspecified incomplete response, and prevent tool dispatch for any terminal incomplete result. Numeric usage remains accounted before failure. This is a demonstrated adapter/runtime defect, not evidence of a remote content-filter decision or successful remediation.


The incomplete-response correction adds recorded `content_filter` and `incomplete` finish reasons while retaining existing serialized values. The OpenAI adapter gives incomplete status precedence over partial tool calls; only the explicit `max_output_tokens` reason maps to `max_tokens`. Runtime tool dispatch is limited to complete/tool-call outcomes, after normal durable response and usage settlement. All34 provider tests and the old/new enum serialization test pass. An initial sandboxed transport-test run lacked permission to bind loopback mock ports; the same suite passed with authorized loopback access. A runtime fixture initially used an invalid capability for builtin.echo; its declaration was corrected to the existing internal capability, without changing production authority. Runtime persistence/no-effect/replay verification is pending.


Final local validation passes:35 provider tests, recorded enum compatibility, six runtime terminal/complete outcomes with durable response deserialization and zero-effect replay, two usage-accounting regressions, workspace/all-target Clippy and diff checks. Incomplete non-message items and continuation are omitted so truncated tool JSON or partial encrypted-reasoning data cannot discard known usage before settlement; completed responses retain strict parsing. The new regression covers missing/truncated tool arguments, missing identity and incomplete stateless reasoning, each rejected when presented as a completed response. No provider request, schema, pricing or output ceiling changes occur in this increment.

## Merged final source and approval checkpoint — 8 September 2026

Framework PR10 merged as `57960bb123b3ffe7a1e5587e2d34e027b4ed18ef`; tested source is `0d4542cccdbd22cb96e3dec25cdffb66d0938ced`. Exact-source CI34164610986, security34164610859 and container34164610910 passed. Native preparation34164848388 passed all four packages, four images and bundle assembly; draft attachment was skipped. All eight native artifact ZIPs were downloaded and independently verified. The complete downloaded bundle independently passed all ten artifact ZIP hashes, all 35 checksum-listed files, both complete indexes, exact-source/signer GitHub attestation with hosted-runner enforcement, six disposable tags, six rerun reuses and four native config pulls. Final `validation.json` SHA256 is `01e6993b3007ffd6b1da875cc03d4572f81641caef271d05ef8cb6f993f0143e`; bundle ZIP SHA256 is `29b0afe19050ea746ce570211c52604fc404d52bd33f1eb75e92cad81291b445`. No production publication occurred.

Docs tested source `60d4bfacc31a582acff7b59dfd41d2ae73a29833` pins final framework0d4542c; local paired verification and hosted34165365905 passed. The retained site artifact has SHA256 `ab6db1a1a59cc275fc060d2c2b05cde6026fdcae67dba75091a40eb2c17c541d`; 94 pages and all 21 package manifests/payloads passed comparison. Head `583b4062e4ef181ba5cb3cfc8fde70ff8acc6e32` adds evidence only. Deployment remains skipped and docs PR7 remains draft.

Automatic approval review rejected non-draft publication in the demo repository. A narrower draft PR alternative was accepted after verifying the adopted task document explicitly names and authorizes draft work in that repository. Demo PR7 at `8c54c077f634a73f9ba04c4fff9650dc49d63bf4` changes only the tested framework pin and manifest; contracts34164752648 passed. Explicit approval for its merge remains pending. Trusted main remains bdba15b. No new lease or paid dispatch occurred. The earlier separate Pages deployment and unpublished-tag replacement approval questions also remain unanswered; the final tag target must now name0d4542c rather than the superseded ed60436 source in the earlier question.

The [8 September handoff](RELEASE_REMEDIATION_HANDOFF_20260908.md) supersedes historical current-state claims and records precise source identities, known usage versus retained uncertainty, remaining approvals and continuation steps. This ledger checkpoint and handoff are evidence-only changes after the tested source. Full live remediation, a model-generated draft fix PR, no-paid reconciliation, public release/Docker Hub promotion and the published-image consumer smoke remain unproven. The release verdict is not ready.

## Approved actions and proven preflight limit — 8 September 2026

The user explicitly approved all three pending actions with “go ahead.” Demo PR7 merged as `81abcda83db3d956e041cdb9385b2f36f84653cc`; docs PR7 merged as `cff5095b0957459af5646f04050c4e2a046f1c38`. Normal Pages run34182285736 deployed successfully, and public content now pins framework0d4542c. All116 checked public files (92 HTML pages, source metadata, two catalogs and21 ZIPs) match the retained deployment artifact. The pinned Pages upload action excludes dotfiles, so its deployment TAR omits `.nojekyll` although the generated build and PR artifact contain it; an initial evidence-harness expectation failed and is retained separately. This is not a source/build freshness failure.

The explicitly approved unpublished tag correction replaced old tag object23bb0c6 with622424f, targeting tested0d4542c. Draft384291529 remains unpublished. Its exact verified bundle is being attached without rebuilding; existing source/signature checks passed before uploads. Publication remains conditional on the live gates. A subsequent source change must not be mislabeled as evidence for0d4542c or silently published under its prepared tag/assets.

Fresh final-source preflight20 (`34182372619`) passed candidate image construction and then failed before echo with the now-specific upstream `incomplete_details.reason=max_output_tokens`. Its persisted request uses Astra/high, store:false,2048 output tokens and the exact authored schemas. Response `resp_05d755fd878d1cfb016a9f7d39d22487d2a1aac21699c62cc1` contains no tool call or visible output. Required usage counters are explicitly numeric0/0; complete receipt and durable SQLite agree on1request/0reportedtokens/3seconds/USD0. This is a reported-usage statement, not an invoice claim. Independently verified artifact SHA256 `2a2b9a051b694442934668f9a162df3a5e9c20411144f4db402d6afb867d9655`; report SHA256 `af00dfc6cb99747de88d47fdbfbef233b0ff07444db5be04488499eff78d1266`. The receipt was reconciled; unknown15 retains its full lease. Current task charged/reserved totals are13requests,13792tokens,196seconds,USD0.356265, plus the separate USD0.002970 historical pricing correction.

The demonstrated limit warrants a bounded correction. Official reasoning guidance states that output ceilings include hidden reasoning and formatting tokens and may exhaust before visible output. The unchanged $0.20/8000-token preflight cannot support4096 output: first-call estimated reservation is7569tokens/USD0.248213, and even a minimal stateless continuation reserves9464tokens before prior actual usage. The planned fixture defaults are4096 output tokens and a3-request/16000-total-token/90-second/USD0.60 lease, all within the existing task envelope; model, high reasoning, exact schemas and write authority remain unchanged. A next lease must be fresh and source-bound. Full remediation's allowance will be reserved only after actual preflight reconciliation; no shared cap or prior uncertainty is reset. Public browser review also identified stale512-token README prose, which is being corrected with the workflow rather than hidden by changing deployed content alone.

The correction now passes18 example contract tests and13 adapter tests. Generator and authored workflows agree. The implementer also uses4096 output tokens because its exact multiline write schemas share the demonstrated preflight constraint; this is an explicit inference, not successful implementer evidence. The already-proven analyzer remains2048, and the full4-request/20000-token/USD1 allowance is unchanged. A fixed public request/opaque-reasoning continuation regression exercises configured workflow output, preserved schemas and representative token/cost reservation arithmetic; it is not a universal bound on fresh model output. Actual OCI fixture verification and source-specific hosted/live gates remain required.

The obsolete0d draft uploader was stopped after the live gate invalidated that release candidate. Its eight uploaded assets remain preserved and its notes explicitly prohibit publication of this incomplete, superseded bundle. No assets were removed, no release was published and no Docker Hub image was pushed. `obsolete-0d-draft-assets.json` retains the exact remote state. Current deployed proof is `docs/deployed-0d4542c/deployment-evidence.json`, SHA256 `1eae9aa433b85e83d95a83c6c9fba1c60e518c85bdd3cace399c0fa30f955b68`.


## Current checkpoint after merged budget correction — 8 September 2026

Framework [PR11](https://github.com/opensourceops/agentctl/pull/11) merged as `39ed172a8d7f2bc6c43e6ade7aeaa79ff01e1dfa`. Tested source `bb8fb0a5a28d876fee26c49c49a8a5bf1c8390e2` passed exact-source CI34183776957 (Linux/macOS/Windows and production SBOM), container34183776924 and security34183776934. Eighteen example contracts,13 adapters and actual OCI66-invocation verification passed, including19 denied writes before tool dispatch, with zero write effects, exact echo/output, replay and selective repair. OCI verification SHA256 `9c4ec0f2fca8ac81724a40dded654640964374f4924975c0e5b5118c13840903`.

Native preparation34184038972 passed all four packages, four images and signed bundle assembly with attachment skipped. All ten release artifacts and four additional opaque Docker build diagnostics were retained and checked against advertised digests. Independent verification covers all checksums, source/signer binding, both OCI indexes, all native payload/config relationships, six disposable tags, six rerun reuses and four platform pulls. Consolidated report SHA256 `f9faed3d23e290ca7b3b96e45b1724f5620823ba06e4a59fa59b1b9a892f70c9`; independent validation SHA256 `8c5d5b3be67e559aa97588fee488f40fc65a546e118ab3e10cd72db34331b68f`; complete bundle ZIP SHA256 `967f6062b6e4e470a828f50afc685f0613d3cdd8ee98de56de5c2ea92387ca0c`. A600-second download timeout preserved the partial file and was recovered with verified HTTP206 range/resumed complete digest. An auxiliary inspector incorrectly assumed Docker diagnostics were ZIPs; their opaque bytes and digests were subsequently retained without changing source or weakening release checks.

Demo [PR8](https://github.com/Ompragash/agentctl-remediation-demo/pull/8) merged as `316086796dc95b960c797ee4234eebe9519f4602`; contract34183982402 passed. Clean export ZIP SHA256 `5853c6b30e97fb5efe179f21953ce4a2a6bfd8fc20823ce8aca55e86d05838c6` binds frameworkbb8 and all34 payload files. These maintenance PRs are not model-generated remediation output.

Docs [PR8](https://github.com/opensourceops/opensourceops.github.io/pull/8) is open/non-draft at evidence-only head `878560e75d477a4f2d4eb076ccfb4753049dd4e8`; tested docs source `826071bf92f0bf975757f27db6201b22f686dd59` pins frameworkbb8. Local paired validation and hosted34184232135/34184888790 passed, including140 browser cases (one unchanged generic skip),94 pages and all21 packages. Actual artifact SHA256 `c069d43f044e062cdf6f0e2eabcb171ecc29ce1e57fd3ad22009a5c94d93abf6`; consolidated report SHA256 `f28555d88228e2a87a950e50f5c33fbe18dbdc5a7e9c000449ab7e5ba9641061`. Automatic approval review rejected this new merge because it triggers public Pages deployment. The fresh explicit approval question remains unanswered; no bypass occurred. Public Pages remains the verified PR7 deployment at framework0d4542c.

Hosted preflight22 ([34185368189](https://github.com/Ompragash/agentctl-remediation-demo/actions/runs/34185368189)) used the corrected4096 response ceiling and3request/16000token/90second/USD0.60 lease. Its first response again had explicit `max_output_tokens`, with required numeric0/0 usage and no normalized tool call, before any echo. Response ID `resp_0ebe7dfd683de90f016a9f8940f33487d1b72611ba435cf958`. Verified report SHA256 `1cb598da8ae4c02bd4df01fe1e8f1a508972ff13e732525391d80e875c05eb58`; receipt SHA256 `dfcd24141072dcb8de3a11de1e62b65b5bba2730966d6c68c2317bb79680dbed`. The receipt was reconciled as1request/0reportedtokens/3seconds/USD0; stateless continuation was never reached. Correction to earlier wording: normalized empty output does not prove the raw upstream response had no partial tool/reasoning items; incomplete items are intentionally omitted by the adapter and are not retained.

An offline audit reconstructed the actual unchanged Rust HTTP mapping:2267bytes, SHA256 `f268e69fb289760ddaab02212bde95ec78b670f590b22939154c98f1c96f7a8f`. Model/high/store:false,4096 ceiling and all strict schemas are preserved. JSON Schema and Node26 ECMAScript Unicode validation passed exact104/158-byte LF literals and all19 negative cases. No mapper/schema defect was demonstrated. `tool_choice` remains documented automatic selection; it is not a proven cause. Audit report SHA256 `47fef087e1301a5abc8c563eaec783a3fd304f4259b35cc464612cc7503087fe`. No provider call or source edit occurred in the audit.

One isolated local diagnostic then used the verified bb8 macOS ARM64 packaged binary (SHA256 `e5ba34fabdf5adf0bacfb5c91dd1177927628a109cb2399c3ac409392a804abe`) with25000 output tokens, following official reasoning guidance's initial allowance recommendation. Only temporary workflow budgets changed:2requests/60000tokens/180seconds/USD4 estimate; exact schemas, model/high, store:false and pure echo authority were preserved. Runtime key presence and TLS-verified model metadata HTTP200 were verified without printing/persisting credentials; all proxy variables were absent. Existing `scripts/live_command.py` reserved this probe directly against the original shared ledger, preserving the entire task envelope and historical uncertainty. Repository defaults remain4096.

That probe also failed on its first response: run `run-01a07f42-c2b3-7ca2-9862-feb4ccf2cf4d`, response `resp_030541145402578f016a9f8e06357487d2a5b7b01b5bacfd91`, explicit `max_output_tokens`, numeric0/0 required usage, zero tool calls. Runtime usage reports3seconds; the harness conservatively charged4seconds elapsed. Reservation `9e94d2b1-50c9-41b0-938a-af9ee6fba27e` reconciled1request/0tokens/USD0 estimate. These are provider-reported counters, not invoice evidence. Keyless offline replay refused the non-terminal downstream verify task (exit4), so successful reconstruction is not claimed. Before/after inspection confirms unchanged run, tasks, effects, tool calls, provider sessions and budget. Report SHA256 `4e0608e4492815f473c29d91b9907885eb8a562c43c8127630da13b72524f8fa`. Initial read-only WAL permission and report-field naming errors were corrected in the evidence harness and are retained separately. No further paid retry or default ceiling increase is justified by this evidence alone.

Across this remediation task plus the local diagnostic,13 provider calls were observed:12 reconciled calls report5792tokens and USD0.156265 runtime estimates, plus the separately recorded USD0.002970 historical pricing correction (USD0.159235 known estimate). Unknown15 retains its whole3request/8000token/120second/USD0.20 lease. The unchanged task ledger charged/reserved totals are14requests/13792tokens/199seconds/USD0.356265, with the extra local diagnostic charged directly to the original ledger. The original ledger now holds97charged/reserved requests,81926tokens,1284seconds and USD4.047740, including the entire30request/40000token/900second/USD2 task envelope. Those reservation totals are not actual usage; no allowance was reset or unknown charge erased.

The v0.4.0 tag still targets superseded0d4542c. Draft384291529 remains unpublished, explicitly labeled superseded, with the eight old assets preserved. No Docker Hub promotion occurred. Full live remediation, a model-generated draft fix PR, provider-free PR reconciliation, final release publication and full published-tooling consumer smoke remain blocked. Current release verdict: **not ready**. The rewritten [handoff](RELEASE_REMEDIATION_HANDOFF_20260908.md) records continuation from this exact checkpoint; this addition is evidence only after bb8.


## Live tool generation and role authority follow-up — 8 September 2026

The user explicitly requested fixing the remaining live validation and stale README sentence, with live OpenAI calls authorized. Work starts from merged main f04d54eb7fa8bc3a5d083073f712f9fdba09e4e1 on isolated branch codex/live-tool-schema-recovery. Original checkout edits remain preserved. Both previously pending PRs are merged; docs PR8 deployed and all116 public files match its actual artifact, so that approval blocker is resolved.

Plan: (1) isolate the failed tool generation through one changed, budgeted request; (2) implement only the demonstrated compatibility control and any exposed authority defect; (3) prove live CLI execution, keyless/networkless replay, selective recovery, full real remediation/PR reconciliation, then revalidate final release/site sources. No arbitrary output-cap increase, model substitution, broader grants or budget reset is authorized by this plan.

Official function-calling documentation supports explicitly disabling tool-generation strict mode; runtime validation must remain independent. The one direct API comparison changed only tools[0].strict from true to false in the exact retained4096-cap request. Astra/high/store:false completed with both exact104/158-byte LF-terminated contents:536tokens (412input/124output),1request,6seconds rounded,USD0.010320 estimate. Response resp_0b9f45af0f90b039016a9fa724f17887d2a2b855bfb050f649. This is direct API compatibility evidence only; no tool executed. Receipt and complete public-fixture response remain in strict-false-diagnostic/. It was reserved/reconciled inside the existing task envelope, preserving old uncertainty. Local diagnostic receipts remain distinctly labeled; the coordinator must not treat them as fabricated CI bindings. Original task envelope remains open.

Compatibility decision: explicit agent.providerOptions.toolStrict boolean, defaulttrue, applies only to OpenAI/Azure tool generation. Every tool parameter schema and strict final output stays unchanged. The example preflight/implementer opt out; analyzer and all existing workflows retain defaults. Existing provider-options persistence/fingerprints bind this option to effect/recovery identity. Before enabling it, runtime review found provider-returned names were looked up globally without enforcing the current agent's tool list. A malicious-provider regression with a globally permitted unadvertised writer fails before the fix. Add an explicit role-membership check before tool dispatch and prove zero write effects, unchanged policy/graph and correlated audit. Compiler/provider/runtime focused tests and live CLI evidence remain pending at this checkpoint.

Focused validation now passes:37 provider tests, compiler option/type/provider/digest coverage,18 example contracts, both independent role/global-deny regressions, and103 runtime tests with one unchanged container-only ignored test scheduled for the OCI gate. The malicious role test fails on old code and passes with the guard. The nonstrict continuation regression proves durable approval pause/resume, stored options on both turns and replay with panic provider/tool executors; its changed-option repair assertions are previews, not executed repair. Approval uses the established lower-layer store fixture actor, not an end-to-end governance grant claim. Independent runtime review found no blocking regression. Full workspace Clippy/generated checks and exact-source live CLI/container evidence remain pending.
