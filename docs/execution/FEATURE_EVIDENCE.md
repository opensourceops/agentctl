# Launch-readiness feature-to-evidence matrix

The subsequent PR review is complete at framework code `4a22f7f733c5c722263b956b59f36107ec398fc7`, paired with docs code `8b3cdda50dea34a7282365f0d09dc7eef219848c`. See the [current review report](PR_REVIEW_REPORT.md) for all ten findings, twenty direct journeys, final hosted gates, artifact digests and separate runtime/documentation verdicts. The tables below preserve the earlier launch evidence and its exact source labels.

Final executable source: `ce94443a6b0a840eb9cd0e5f5b2e128dfe8f15c8`. **Launch-ready pre-1.0 candidate for review.** Full Linux/macOS/Windows CI and RC, package, production/image SBOM, container, supply-chain and paired documentation gates passed. Final source/artifact bindings and the later evidence-only commit scope are in [the execution ledger](AUTONOMOUS_LAUNCH_READINESS.md) and [machine-readable final evidence](LAUNCH_FINAL_EVIDENCE.json). The [limitation review](LAUNCH_LIMITATION_REVIEW.md) preserves deliberate boundaries. Earlier checkpoints below remain source-specific history.

The first table records implemented additions and named, observed evidence. The later baseline inventory is intentionally historical: its **T** (test inspected), **C** (CLI scenario), **S** (static), **H** (historical execution), and **G** (then-missing coverage) labels describe source `ec7e220820ed169c005aff5344d81fe4d292cdb6`. Resolved baseline gaps are superseded by the continuation table; they are not current missing-feature claims.

Commands run from the framework root. **Core**, **Runtime**, **Store**, **CLI**, **Providers**, and **Xtask** test references mean `cargo test -p agentctl-core TEST --locked`, substituting the corresponding crate (`xtask` for Xtask). **CLI integration** means `cargo test -p agentctl-cli --test sources_cli TEST --locked`. Python test references identify their source module; execute the named script with `python3 PATH` to run that credential-free suite. Named filters below have been checked against current source definitions. A compiled Windows test is not an executed Windows test.

Evidence filenames below identify retained task artifacts, not public downloads. They are stored in the task's `work/evidence/`; publishable final links and digests will be recorded in the execution ledger. Do not rename earlier logs to a later code-under-test SHA.

## Evidence checkpoints

| ID | Observed evidence | Source and qualification |
| --- | --- | --- |
| D1 | `final-verify-ec89c29.log`: full `cargo xtask verify` passed | Implementation `ec89c29591c905e41cbf6e8d9559ac321b8f5488`; generated schema followed in `266eb44360886aa3019e5d42eb9cd28d26f97246`. This is prior-source full-suite evidence. |
| D2 | `final-acceptance-266eb44.log`, `final-completeness-266eb44.log`, `final-container-266eb44-ca.log` passed | Source `266eb44360886aa3019e5d42eb9cd28d26f97246`; 46 packaged acceptance scenarios, composite execution, and actual OCI capture/approval/deleted-source resume/network-disabled replay. |
| D3 | `runtime-hostile-tool-focused.log`, `runtime-vars-recovery-focused.log`, `runtime-adversarial-clippy.log` passed | Two tests-only additions first executed over `29429de`; source-file hash and dirty-worktree identity are in `runtime-adversarial-focused-results.json`. Tests were then committed in `0eef26a`; do not relabel the original logs. |
| D4 | `devops-final-0eef26a.json`: all 20 clean-directory deterministic cases passed; `devops-container-final-0eef26a.json`: case 04 real container build passed | Clean source `0eef26a`; binary SHA-256 `3eccd5bef3891a27392935c7f95c57ed9fcde20cf17f23941bd5a94b2cf0ba76`. Reports retain run IDs, commands, artifacts, semantic/denial/replay assertions and fixture labels. Later clean local execution of all 20 cases is recorded in `devops-final-9c4f6e0.json`; D5 records actual Windows execution of all 20 at `db7b59b`. |
| D5 | All four `source_windows_` tests **executed and passed** on hosted Windows, including in-place attribute-only junction mutation; all 20 DevOps cases also passed there | Source `db7b59b`, [CI run 33998511938](https://github.com/opensourceops/agentctl/actions/runs/33998511938), retained `hosted-failed-33998511938.log` lines 983–996 and 1547–1566. Windows core suite: 77 passed. The overall verification gate later **failed** in six Python budget-fixture cleanup cases with open SQLite connections. Earlier `windows-source-cross-check.log`/Clippy remain compilation-only history. |
| D6 | `live-harness-final-tests.log`, `live-metadata-tests.log`, earlier `live-helper-tests.log` | Deterministic allowance/wrapper/xtask checkpoints have differing inventories; the latest wrapper includes numeric failure metadata and the latest xtask tests cover parallel output reservation and partial-summary semantics. D7 records the final integrated suite pass. |
| D7 | Final complete Linux/macOS/Windows CI and RC passed; eight hosted ZIP digests and inner package checksums independently verified | Clean `ce94443`; CI33999337160, RC33999340847, container33999337167 and security33999337162. This supersedes earlier pending integration/platform gates. All four Windows source tests and all twenty examples passed in the complete gate. |
| D8 | `devops-final-ce94443.json`: all20 local isolated cases passed; `devops-container-final-ce94443.json`: actual disposable case04 build passed | Clean `ce94443`; [validation.json](../../examples/devops/validation.json) retains exact workflow/binary/report identities and failures alongside passing live coverage. |
| D9 | Paired docs hosted run33999609203 and actual downloaded ZIP passed | Docs code e2f7664/head d4b79f8 pin exact framework ce94443;280 files/73 HTML/64 imports, hidden marker and source hashes verified, deployment skipped. |
| L1 | `final-live-budget-266eb44.log` passed with `gpt-5-mini` | One real request, 18 input +42 output tokens, then pre-dispatch provider-request budget denial; source `266eb44`. |
| L2 | `final-live-astra-29429de.log` passed with `gpt-6-astra` | Source `29429de`: two local +two OCI requests, 987 input +66 output tokens, verified tools/continuations and two keyless replays. |
| L3 | Successful Sol prefix and focused local/OCI composite/recovery assertions; complete commands also had failures | Prefix at `29429de`, continuation at `0eef26a`. See the live-status section below; no whole-command pass is claimed. L4 records the separately completed new-example inventory. |
| L4 | All four distinct `gpt-5-mini` DevOps variants passed across source-labeled focused runs | Cases 01, 12 and 19 passed at clean `db7b59b` in `devops-live-final-db7b59b.json`; case 20 passed at clean `9c4f6e0` in `devops-live-20-final-9c4f6e0.json` after its strict-schema representation fix. Nine successful requests, 4,178 input+output tokens. The earlier complete invocation still failed at case 20 and remains recorded. |

## Added and strengthened contracts

| Contract and implementation | Runnable positive evidence | Runnable negative/failure evidence | Observed evidence / remaining gate |
| --- | --- | --- | --- |
| Ordered workflow, agent, task and invocation `varsFiles`; shallow top-level replacement; inputs remain separate. `crates/agentctl-core/src/sources.rs`, `dsl.rs`, `compiler.rs`; CLI variable arguments | Core `ordered_files_replace_whole_keys_and_keep_input_namespace_separate`; CLI integration `ordered_files_cli_overrides_origins_and_replay_from_another_directory` | Core `source_types_encoding_size_duplicate_keys_and_includes_fail_closed`; CLI integration `duplicate_and_reserved_variables_fail_before_creating_run_state` | D1, D4 case 11. Low-to-high: workflow files → workflow inline → agent files → agent inline → task files → task inline → invocation files → invocation inline. Later repeated files/flags win; objects, arrays, scalars and null replace a whole key. |
| Reserved bindings and passive ordinary data. `crates/agentctl-core/src/sources.rs`, compiler/template context | Core `reserved_bindings_are_rejected_but_ordinary_data_does_not_dereference_secrets` preserves ordinary `{env: ...}` and `{file: ...}` objects as data | Same test rejects reserved engine keys; Core `compiler_rejects_uncaptured_vars_and_instruction_dependency_escalation` rejects unresolved sources and instruction dependency escalation | D1. No recursive includes, implicit environment import, secret dereference or policy authority from variables. |
| Capture instructions once before compile; bounded regular UTF-8 sources and content fingerprints. `crates/agentctl-core/src/sources.rs`; existing exclusive `instructions`/`instructionsFile` DSL remains | Core `captured_instruction_bytes_are_immutable_and_change_plan_identity`; CLI integration `check_reads_instruction_files_and_reports_missing_source` | Core `source_denials_fail_before_capture_and_do_not_mutate_input_workflow`; `source_types_encoding_size_duplicate_keys_and_includes_fail_closed` | D1. Maximum 1 MiB/file, 256 file references and 16 MiB/capture. User-supplied internal snapshots are rejected. |
| Declaring origin independent of invocation cwd and selected policy workspace; pack source bytes integrity-bound | Core `source_origins_are_independent_from_workspace_and_pack_integrity_binds_bytes`; CLI `local_pack_assets_bind_origins_digests_and_caller_read_policy` | Same regressions reject outside-workspace targets, explicit read denial and declared-digest mismatch | D1. Workflow/pack manifest origins resolve their own files; explicit CLI variable files resolve from invocation cwd. `--workspace` remains the policy base. |
| Unix configuration-source confinement and unreadable/special-file rejection. `crates/agentctl-core/src/sources.rs` | Core `regular_metadata_requires_absolute_regular_file`; source-capture tests above | Core `source_symlink_escape_and_fifo_are_rejected_without_blocking`; `source_unreadable_instruction_file_fails_before_capture`; `source_unreadable_variable_file_fails_before_capture` | D1 plus D5. Unix uses no-follow descriptor traversal; permission fixtures exercise real unreadable files. These results do not establish Windows behavior. |
| Windows source capture uses retained directory handles and one no-follow relative component per open. `crates/agentctl-core/src/sources.rs`; target-Windows cap-primitives/cap-fs-ext | Core `source_windows_opened_parent_cannot_be_renamed_during_capture` checks readable original bytes and rename success after handles close | Core `source_windows_junction_swap_and_restore_cannot_redirect_component_open`; `source_windows_in_place_reparse_mutation_cannot_redirect_opened_parent`; `source_windows_junction_escape_is_rejected_before_capture` | D5: all four tests executed and passed on Windows at `db7b59b`, including in-place mutation. The overall gate later failed in Python budget-fixture cleanup; final integrated verification is now passed in D7. Replaces vulnerable full-path pre/post checks. Sharing-only guards were rejected because attribute-only reparse mutation is not prevented by write sharing. |
| File instructions use inline template typing, local sub-workflow inputs and declared dependencies. Runtime task context | Runtime `subworkflow_instruction_templates_use_local_typed_inputs_and_handoffs`; `captured_instructions_survive_file_changes_approval_resume_replay_and_fork` inspect actual provider request instructions | Runtime `instruction_templates_reject_missing_and_nonscalar_values_before_provider_dispatch`; `instruction_only_input_changes_invalidate_reuse_identity` | D1. Capturing test providers are deterministic fixtures, not live OpenAI observations. |
| Snapshot-backed approval/resume/replay/fork without rereading mutable sources. Runtime/store workflow snapshot | Runtime `captured_instructions_survive_file_changes_approval_resume_replay_and_fork`; CLI integration ordered-files test above; `cargo xtask acceptance-container` | Same runtime case proves no provider before approvals, source deletion/change cannot alter the reviewed request, replay adds no calls/effects, and only explicit fork dispatches fresh work | D1, D2. OCI covers two attributed fixture approvals and network-disabled replay. Encryption follows existing selected-field policy. Legacy snapshotless histories cannot reconstruct never-captured source bytes. |
| Changed variables/instructions invalidate affected recovery reuse and approvals while independent boundaries remain reusable | Runtime `vars_file_change_blocks_retry_and_preserves_only_unaffected_repair_boundaries`; `repair_fingerprints_include_tool_definitions_and_resolved_task_variables` | Runtime `repair_detects_changed_upstream_prompt_file`; new variable-file case rejects identical retry, changed-upstream reuse and unacknowledged restart of a successful root | D1, D3. File-only winning-value change causes new approval/effect/input digest; an independent sibling is reused. Deleted-source resume and zero-effect replay are checked. Explicit fixture actor and policy grants remain enabled. |
| Hostile instructions in tool data cannot add authority or forbidden effects. Runtime tool loop/policy | Runtime `hostile_tool_data_cannot_expand_authority_or_dispatch_forbidden_mutation` completes the allowed reader and passes actual hostile output to the second model request | Same test requests a forbidden writer and proves zero forbidden executor calls, absent file, unchanged stored policy/graph and correlated durable failure/audit/effects | D3. Both allow and deny entries are explicit, so deny precedence is exercised. |
| Manifest-bound assets verified for local, Git and archive packs. `crates/agentctl-core/src/pack.rs`, `crates/agentctl-cli/src/packs.rs` | CLI `local_pack_assets_bind_origins_digests_and_caller_read_policy`; `pinned_git_cache_is_reusable_offline`; `immutable_archive_is_bounded_cached_and_rejects_links` | Core `verifies_declared_assets_and_rejects_tamper_missing_and_invalid_text`; `asset_paths_are_portable_normalized_and_all_source_references_are_bound`; `asset_symlink_escape_and_fifo_fail_without_blocking` | D1. `packs verify` checks all declared assets, including unused entries. Assets are bounded UTF-8 configuration files; source capture verifies the bytes against their expected hashes. Windows read-helper change is subject to D5. |
| Archive acquisition obeys caller URL/IP, proxy, CA, timeout and size policy; fresh HTTPS Git fails closed | CLI `archive_transport_respects_explicit_proxy_setting`; local/offline Git case above | CLI `archive_policy_denial_creates_no_request_or_cache`; `archive_redirect_and_policy_response_bounds_hold_inside_cli_runtime`; `remote_git_fetch_is_rejected_before_cache_or_network` | D1. Reuses ordinary HTTP transport preparation. Fresh Git subprocess fetch is rejected until equivalent DNS pinning is enforceable; contained file Git and exact populated caches remain available. |
| Repeated raw provider tool-call IDs remain distinct across durable effects; populated migration 16 preserves evidence | Store `repeated_provider_call_ids_are_scoped_to_the_durable_effect`; `migration_sixteen_preserves_populated_tool_call_evidence`; `repeated_tool_call_ids_have_a_stable_effect_order` | Store `tool_call_identity_rejects_cross_run_task_and_input_mismatches`; repeated-ID case verifies scoped completion/uncertainty cannot affect the other call | D1. Identity is `(run_id, effect_id, call_id)`; provider continuation retains the raw call ID. |
| Redacted effective winning-variable origins and plan instruction digests; stable source error envelopes | CLI integration `ordered_files_cli_overrides_origins_and_replay_from_another_directory`; `check_reads_instruction_files_and_reports_missing_source` | Same cases assert private sentinels absent from explain/plan; `duplicate_and_reserved_variables_fail_before_creating_run_state` rejects before run-state creation | D1. Origins identify winning layers per task without values; captured bytes remain in protected history according to configured state policy. |
| Honest non-dispatching doctor for credentials and OCI prerequisites. `crates/agentctl-cli/src/preflight.rs`, existing process preflight | CLI `doctor_accepts_a_workflow_path`; Core `regular_metadata_requires_absolute_regular_file` | CLI integration `doctor_missing_credentials_is_nonzero_and_does_not_create_state` | D1, D2. Missing prerequisites return exit 6; executable existence alone does not prove engine/image readiness or model access. Metadata inspection uses the shared confined source opener. |
| Shared paid allowance before dispatch; reconcile actual input/output once; preserve unknown charges | `examples/devops/test_live_budget.py`: `test_independent_callers_cannot_oversubscribe`, `test_reconcile_does_not_double_count_reasoning`; `scripts/test_live_command.py`: `test_run_reserves_before_dispatch_reconciles_actual_without_reasoning_duplication`, `test_resume_reserves_remaining_limits_and_charges_only_delta` | Budget `test_interrupted_reservation_survives_reopen`, `test_allowance_cannot_be_reset_by_later_caller`; wrapper `test_missing_any_budget_rejects_before_dispatch`, `test_actual_overrun_fails_gate_and_keeps_full_actual_charge`, `test_uncertain_or_started_effect_retains_full_charge` | D1, D6, L1. One ledger caps 100 requests, 200,000 input+output tokens, 1,800 paid seconds and USD 25 estimated cost across commands/retries/OCI. Reasoning is an output subset. Unknown accounting retains reservation rather than pretending actual usage is zero. |
| Parallel reservation compatibility, credential-free passthrough, numeric-only failure metadata and partial live inventory | Xtask `live_configuration_preserves_aggregate_parallel_output_reservations`, `parallel_budget_fix_leaves_independent_live_fixture_caps_unchanged`; wrapper `test_effect_free_commands_are_exact_passthrough`, `test_uncertain_usage_retains_numeric_audit_metadata_without_payloads` | Xtask `live_configuration_preserves_stricter_limits_and_non_openai_models`; wrapper `test_environment_credential_is_redacted_from_both_streams`; `scripts/test_container_agentctl.py`: `test_only_real_dispatch_passes_credential_by_name` | D6. The failed Sol composite exposed a real fixture cap conflict; focused continuation preserves previously passed prefix evidence and explicitly reports incomplete legacy inventory. Metadata retains numeric usage/run/effect status without payloads. |
| OpenAI provider request compatibility without implicit advanced cache options | Providers `openai_cache_routing_does_not_enable_model_specific_cache_options`; actual mini/Astra gates L1/L2 | Same request-shape regression keeps advanced options explicit; baseline unsupported-cache-option HTTP 400 is retained as failure evidence | D1, L1, L2. Existing Responses transport remains. Sol successes/failures and all four successful mini variants (L4) are itemized below; other providers/services remain separately labeled. |
| Twenty distinct inspectable DevOps scenarios, exact catalog inventory and one clean-directory runner | `cargo xtask devops-examples`; `examples/devops/catalog.json`, `run.py`, `fixture.py`; D4 maps all IDs 01–20 to actual runs | Runner checks semantic outcomes, trace correlation, zero-effect replay and denied workflows with no forbidden artifacts; `test_fixture_portability.py`: `test_explicit_git_works_with_empty_environment_and_crlf_patch`, `test_package_digest_rejects_changed_line_endings`, `test_run_probe_closes_sqlite_handle_before_cleanup` | D4: all 20 passed on clean local `0eef26a` and `9c4f6e0`; D5: all 20 also passed on actual Windows at `db7b59b`. Real case 04 image build passed at the recorded D4 checkpoint. Git/Python discovery, CRLF and read-only cleanup failures remain historical failure evidence, not skipped assertions. L4 completes the four distinct live variants across recorded source revisions; D7–D8 record final integrated source/platform passes. |

## Twenty-example catalog checkpoint

All twenty cases have README, current-format workflow, bounded permissions, fixtures, semantic assertions and artifact evidence in the [catalog](../../examples/devops/catalog.json) and [suite guide](../../examples/devops/README.md). D4 records all twenty as passed locally in deterministic mode, and D5 records all twenty passed on actual Windows at `db7b59b`; fake-provider cases are explicitly labeled and do not count as paid model evidence.

| ID | Runnable use case | Deterministic checkpoint | Separate live/extra gate |
| --- | --- | --- | --- |
| 01 | [Failed CI build diagnosis](../../examples/devops/01-ci-diagnosis/README.md) | Local `9c4f6e0`; Windows `db7b59b` passed | `gpt-5-mini` passed at `db7b59b` after the classification-schema correction; earlier failure retained |
| 02 | [JUnit test triage](../../examples/devops/02-junit-triage/README.md) | Local `9c4f6e0`; Windows `db7b59b` passed | No artificial provider call required |
| 03 | [Pipeline configuration review](../../examples/devops/03-pipeline-review/README.md) | Local `9c4f6e0`; Windows `db7b59b` passed | No artificial provider call required |
| 04 | [Dockerfile improvement](../../examples/devops/04-dockerfile-review/README.md) | Local `9c4f6e0`; Windows `db7b59b` passed | Real disposable container build passed at `0eef26a`; no performance-savings claim |
| 05 | [Kubernetes manifest review](../../examples/devops/05-kubernetes-review/README.md) | Local `9c4f6e0`; Windows `db7b59b` passed | No artificial provider call required |
| 06 | [Governed Terraform plan analysis](../../examples/devops/06-terraform-plan/README.md) | Local `9c4f6e0`; Windows `db7b59b` passed | No artificial provider call required |
| 07 | [Dependency update assessment](../../examples/devops/07-dependency-update/README.md) | Local `9c4f6e0`; Windows `db7b59b` passed | No artificial provider call required |
| 08 | [SBOM and vulnerability triage](../../examples/devops/08-sbom-triage/README.md) | Local `9c4f6e0`; Windows `db7b59b` passed | No artificial provider call required |
| 09 | [Verified release notes](../../examples/devops/09-release-notes/README.md) | Local `9c4f6e0`; Windows `db7b59b` passed | No artificial provider call required |
| 10 | [Release readiness decision](../../examples/devops/10-release-readiness/README.md) | Local `9c4f6e0`; Windows `db7b59b` passed | No artificial provider call required |
| 11 | [Configuration drift and variable precedence](../../examples/devops/11-configuration-drift/README.md) | Local `9c4f6e0`; Windows `db7b59b` passed | No artificial provider call required |
| 12 | [Incident timeline](../../examples/devops/12-incident-timeline/README.md) | Local `9c4f6e0`; Windows `db7b59b` passed | `gpt-5-mini` passed at `db7b59b` |
| 13 | [Canary evaluation](../../examples/devops/13-canary-evaluation/README.md) | Local `9c4f6e0`; Windows `db7b59b` passed | No artificial provider call required |
| 14 | [Approval-gated local deployment](../../examples/devops/14-local-deployment/README.md) | Local `9c4f6e0`; Windows `db7b59b` passed | No artificial provider call required |
| 15 | [Interrupted deployment recovery](../../examples/devops/15-interrupted-deployment/README.md) | Local `9c4f6e0`; Windows `db7b59b` passed | Fake provider is explicitly a deterministic failure/recovery fixture |
| 16 | [Terminal retry and selective repair](../../examples/devops/16-retry-repair/README.md) | Local `9c4f6e0`; Windows `db7b59b` passed | No artificial provider call required |
| 17 | [Compensated rollout](../../examples/devops/17-compensated-rollout/README.md) | Local `9c4f6e0`; Windows `db7b59b` passed | No artificial provider call required |
| 18 | [Parallel service matrix](../../examples/devops/18-parallel-matrix/README.md) | Local `9c4f6e0`; Windows `db7b59b` passed | No artificial provider call required |
| 19 | [Typed role sub-workflow](../../examples/devops/19-role-subworkflow/README.md) | Local `9c4f6e0`; Windows `db7b59b` passed | `gpt-5-mini` passed at `db7b59b` |
| 20 | [Bounded remediation loop](../../examples/devops/20-bounded-remediation/README.md) | Local `9c4f6e0`; Windows `db7b59b` passed | `gpt-5-mini` passed at `9c4f6e0` after the strict decimal-token fix; earlier HTTP 400 retained |

The case 01 paid response exposed a closed-schema mismatch: its classification string had no enum constraint although the deterministic verifier accepted only the fixture vocabulary. The corrected schema and a deliberately malformed-classification negative case passed in `devops-classification-contract-focused.json` over a dirty `0eef26a` working tree. That report remains focused deterministic evidence; subsequent `devops-live-final-db7b59b.json` records the repaired case 01 live pass. The earlier paid failure and its uncertain extension effect remain recorded.

## Live status and retained failures

- A baseline `gpt-5-mini` response rejected an implicit advanced cache option with HTTP 400. The request-shape regression and subsequent real preflight/resource-budget successes prove the affected fix; the failed request remains charged.
- At `29429de`, Astra acceptance passed (L2). Four independent Sol legacy workflows passed before the full example command failed in the composite. The failure exposed four 512-token concurrent output reservations exceeding a 1,536-token aggregate ceiling; the fixture now uses four 384-token reservations. The incomplete failed run retains its entire 12-request/25,536-token/240-second/USD 2 reservation. This is charged allowance, not known actual consumption.
- At `0eef26a`, `examples-verify-live-openai-composites` passed its local composite and terminal-retry/selective-repair/replay assertions: the retained partial native summary records 17 requests, 2,045 input +339 output tokens, and reused upstream boundaries. Its OCI selective repair also completed before the separate DevOps stage: `sol-container-0eef26a.json` ties supplemental shared-ledger rows to the sequential gate control flow, 5 requests and 1,458 total tokens. This supplemental record does not masquerade as the native summary, which remained at its earlier `local-complete-container-pending` checkpoint.
- The same complete continuation command **failed** at DevOps case 01; `devops-live-failed-0eef26a.json` records the actual error/run trace and source. Cases 12, 19 and 20 were not reached by that invocation. Local/OCI successes remain valid scoped evidence, while the whole invocation stays failed. The subsequent four-variant successes are recorded separately below.
- The shared live allowance snapshot at that failure charged 47 requests, 31,725 tokens, 320 seconds and USD 2.040741 estimated cost, including one unreconciled reservation. These are historical charged totals, not known actual consumption or an invoice.
- At clean `db7b59b`, cases 01, 12 and 19 passed their real `gpt-5-mini` semantic/tool/replay assertions. Case 20 then received a definitive HTTP 400 because the strict tool schema used a quoted JSON document as a string enum. `9c4f6e0` changed only the bounded-remediation representation to a strictly validated decimal token; the isolated case 20 rerun passed with two requests, 641 input +131 output tokens and one tool call. `devops-live-final-db7b59b.json` and `devops-live-20-final-9c4f6e0.json` retain source/binary hashes and run IDs. The successful unchanged cases were not repaid; the original complete invocation remains failed.
- After that focused pass, known reconciled totals are 45 requests, 10,367 tokens, 113 charged wall seconds and 44,363 microUSD estimated cost. The unchanged uncertain reservation adds 12 requests, 25,536 tokens, 240 seconds and 2,000,000 microUSD; aggregate charged allowance is therefore 57 requests, 35,903 tokens, 353 seconds and 2,044,363 microUSD. Distinguish known reconciled usage from the retained maximum reservation. The same ledger continues across remaining gates.

Do not rerun previously passed unchanged paid workflows just to obtain another green whole-suite label or recover deleted temporary histories. Retain exact source/model, named semantic assertions, actual usage versus unreconciled reservations, and partial coverage. Temporary legacy databases are deleted after assertions; the aggregate log/ledger establishes executed evidence but is not a replayable raw database for later inspection. The examples retain reviewable reports and support `--keep`.

## Earlier final-source checkpoint (superseded by D7–D9)

The Windows source correction and all four distinct mini workflows now have actual execution evidence at the recorded revisions. At this earlier checkpoint, the final connection-lifecycle correction still needed its committed source identity and affected full gates. D7–D9 now record their completion. Required remaining observations include a complete passing Windows gate after the six Python budget-fixture cleanup errors, applicable final Linux/macOS/package/RC/security/container results, final package/archive checksums, source/image/SBOM relationships, retained model accounting, and the paired documentation pin. Do not relabel earlier whole-command failures or repeat unchanged successful paid cases merely for a single green inventory run. An active gate is not an implementation defect; a failed gate is not an environmental pass.

Earlier platform failures are preserved: source `266eb44` failed Linux/Windows fixture portability; `29429de` passed Linux/macOS/security but Windows exposed Git discovery, CRLF and SQLite cleanup issues subsequently fixed. At `db7b59b`, all four source-race tests and all twenty DevOps cases passed on Windows, then the full gate failed in six budget-test cleanup cases because SQLite connections remained open. Those scoped passes do not establish full Windows verification success; the lifecycle correction must preserve the budget assertions and pass the complete gate. The original missing-key/no-engine blockers are resolved: runtime credential, POST/TLS and a usable OCI engine have been exercised. No Anthropic/Gemini/Azure/cloud/real MCP/A2A/production-deployment live coverage is inferred.

Paired site evidence for `0eef26a` passed [hosted validation](https://github.com/opensourceops/opensourceops.github.io/actions/runs/33997148424) and downloaded-artifact verification, with public deployment skipped. `docs-hosted-artifact-verification-0eef26a.json` binds site code `e813d1650731a89bfb7cc83eca7762491dad7a5f`, later head `e8a8ca7d9c50d5a08f2c693e04fa489ac731b527`, ZIP SHA-256 `c4451ea0d1b68529518076e81c9fc4412b119c06592a61514752d700ca7615a1`, `.nojekyll`, 73 HTML pages and 280 files. A previous artifact omitted `.nojekyll`; that was fixed rather than ignored. The site must be repinned and reverified for the next framework source; this prior artifact is not final-head evidence.

Doctor and the discoverable example runner are the two selected product improvements from the requested candidate list. Explain is part of the required variables contract; shared suite budgeting also fulfills a mandatory validation requirement, so it is not counted as a third independent enhancement. No performance or cost savings are claimed without before/after measurements.

## Historical baseline inventory

The following rows preserve the inspected contract at `ec7e220`. **They are not final-candidate execution results.** Abbreviated implementation paths such as `core/src/...` mean `crates/agentctl-core/src/...`. Then-missing variables/capture/pack/live-suite/example items are resolved or qualified by the continuation rows above.

| Supported claim | Production implementation | Runnable positive evidence | Runnable negative/failure evidence | Inventory level / qualification |
|---|---|---|---|---|
| Strict versioned YAML, diagnostics and migration | `crates/agentctl-core/src/dsl.rs`; CLI migration | `cargo test -p agentctl-core parses_versioned_workflow`; `cargo run -p agentctl-cli -- check examples/v1/hello.yaml` | `cargo test -p agentctl-core rejects_unknown_fields_with_location_and_path`; `rejects_retired_v1alpha1_workflow` | T/C; legacy translation remains limited |
| Deterministic bounded graph and explicit dependencies | `crates/agentctl-core/src/compiler.rs` | `cargo test -p agentctl-core stable_order_uses_declaration_order_for_ready_tasks` | `cargo test -p agentctl-core rejects_cycle` | T; deterministic graph does not make fresh model text deterministic |
| Typed templates and dataflow | `core/src/template.rs`, `compiler.rs`; `runtime/src/lib.rs` | `cargo test -p agentctl-core exact_template_preserves_object_type`; `cargo test -p agentctl-runtime deterministic_dataflow_condition_and_working_memory` | `cargo test -p agentctl-core embedded_object_is_rejected`; compiler dependency validation | T/C; null is distinct from missing |
| Agent defaults and task variable overrides | `core/src/compiler.rs:1219`; runtime task context | `cargo test -p agentctl-runtime task_vars_render_agent_defaults_overrides_and_dependency_outputs` | `cargo test -p agentctl-runtime repair_fingerprints_include_tool_definitions_and_resolved_task_variables` | T; ordered variable files absent at inventory checkpoint |
| External instruction files and exclusive source choice | `core/src/dsl.rs:1755`; `runtime/src/lib.rs:6331,7685` | `cargo test -p agentctl-runtime repair_detects_changed_upstream_prompt_file` executes file-backed source | Same regression rejects stale upstream reuse | T/G; no check/plan capture, file-template, pack-origin or approval-race coverage at checkpoint |
| Preview/check/diff without mutation | runtime action execution; CLI run | `cargo test -p agentctl-runtime check_diff_does_not_mutate_and_interactive_approval_resumes` | Same test asserts absent mutation before approval; `cargo xtask examples-verify` verifies absent preview artifact | T/C |
| Explicit policy grants, independent provider/tool checks, deny precedence | `core/src/policy.rs`; runtime effect preparation | `cargo test -p agentctl-core provider_and_tool_allowlists_are_evaluated_independently` | `cargo test -p agentctl-runtime tool_approval_override_cannot_bypass_global_policy_denial` | T/C; assert denied effect has no tool dispatch |
| Durable noninteractive approvals and resume | runtime `prepare_effect_with_approval`, store approvals; CLI approvals | `cargo test -p agentctl-runtime parallel_approvals_pause_and_resume_all_tasks` | `cargo test -p agentctl-runtime non_interactive_run_pauses_without_bypassing_required_approval` | T/C; governance actors/grants must remain explicit fixtures |
| Workspace containment and symlinks | `core/src/policy.rs` | `cargo test -p agentctl-core permits_nested_missing_paths_below_a_writable_root` | `cargo test -p agentctl-core rejects_parent_traversal`; `rejects_symlink_escape` | T; configuration capture needs its own race-safe regular-file checks |
| Secret references, policy and redaction | `core/src/secret.rs`, `dsl.rs`, `policy.rs`; adapters | `cargo test -p agentctl-core parses_bounded_file_and_process_secret_references`; packaged acceptance scenario 32 | `cargo test -p agentctl-core secret_file_and_process_references_require_explicit_policy`; `cargo test -p agentctl-runtime file_secret_subprocess_output_is_redacted_and_never_persisted` | T/C; ordinary vars must not become a secret mechanism |
| Missing credentials fail before provider/state effects | CLI registry/auth, provider constructors | `cargo xtask acceptance` auth fixtures use explicit fake credentials | `cargo test -p agentctl-cli registry_preflights_only_reachable_provider_file_credentials`; packaged missing-key scenario | T/C; credential presence does not prove account/model access |
| Exact network policy and destination preflight | `core/src/network.rs`, `policy.rs`; provider/protocol HTTP | `cargo test -p agentctl-core network_policy_can_explicitly_allow_private_addresses` | `cargo test -p agentctl-core network_policy_enforces_origin_and_every_resolved_address`; `network_wildcard_does_not_match_apex_or_suffix_attack` | T/C; proxy opt-in delegates DNS/routing to the proxy |
| Bounded host processes and honest isolation | `runtime/src/process.rs`; `core/src/dsl.rs` | `cargo test -p agentctl-core process_isolation_defaults_to_host_and_validates_container_contract` | `cargo test -p agentctl-runtime subprocess_output_limit_is_structured_durable_and_secret_safe`; `subprocess_timeout_is_durable_uncertainty_and_blocks_resume` | T/C; process allowlisting is not an OS sandbox |
| Real digest-pinned action container execution | runtime process engine path; xtask container | `cargo xtask acceptance-container` | `cargo test -p agentctl-core rejects_weak_or_misapplied_container_isolation`; packaged missing-engine scenario 45 | T/C/H; usable engine and actual image execution required |
| Provider abstraction and capability rejection | `core/src/provider.rs`; `providers/src/lib.rs`; compiler negotiation | `cargo test -p agentctl-core accepts_stateless_openai_continuation_for_tool_using_agents` | `cargo run -p agentctl-cli -- check examples/v1/capability-failure.yaml` (exit 2) | T/C; Azure/Anthropic/Gemini remain mock-tested |
| Strict tool output and bounded model loop | runtime `execute_agent`; provider/tool contracts | `cargo xtask acceptance` copied mock-tool journey | `cargo test -p agentctl-runtime agent_tool_loop_validates_tool_output_before_model_continuation` | T/C; malformed tool output must not reach model continuation |
| Structured outputs and typed handoffs | compiler output schema validation; runtime completion | `cargo run -p agentctl-cli -- run examples/v1/structured-handoff.yaml --db work/handoff.db` | `cargo test -p agentctl-runtime task_output_contract_failure_is_durable`; `cargo test -p agentctl-core rejects_invalid_task_and_agent_output_contracts` | T/C; role visibility fixed by agent tools |
| Stateful/stateless OpenAI tool continuation | `providers/src/lib.rs`; runtime provider sessions | `cargo test -p agentctl-runtime stateless_tool_continuation_survives_pause_resume_and_recorded_replay` | Provider malformed-reasoning/continuation tests in `cargo test -p agentctl-providers` | T/H; real OpenAI validation separate from mocks |
| Durable bounded streaming | runtime stream sink; OpenAI SSE adapter; CLI JSONL | `cargo test -p agentctl-runtime streaming_is_bounded_redacted_validated_and_copied_by_replay` | `cargo test -p agentctl-runtime interrupted_stream_is_uncertain_and_never_reconnected`; `streaming_cancellation_preserves_accepted_events` | T/C/H; 256 events/task attempt, 4 KiB payload, 8 MiB SSE |
| Request/token/tool/cost/resource budgets | core budgets; store atomic reservations; runtime reconciliation | `cargo test -p agentctl-runtime provider_request_budget_stops_before_second_dispatch`; `cargo xtask resource-budget-live-openai` | `cargo test -p agentctl-runtime actual_token_usage_is_reconciled_and_terminates_the_run`; `process_output_budget_denies_before_process_dispatch`; `artifact_budget_denies_before_cas_ingestion` | T/C/H; live-suite aggregate limits are a separate G |
| Durable wall-time budget | runtime durable creation timestamp/cancellation | `cargo test -p agentctl-runtime wall_time_budget_uses_original_creation_time_after_resume` | `cargo test -p agentctl-runtime wall_time_budget_cancels_in_flight_provider_and_fails_the_run` | T; paused elapsed time counts |
| Bounded deterministic parallel scheduling | compiler memory conflict checks; runtime scheduler | `cargo test -p agentctl-runtime parallel_scheduler_overlaps_work_caps_concurrency_and_commits_in_plan_order` | `cargo test -p agentctl-runtime parallel_dynamic_memory_write_is_rejected_before_effect_dispatch`; `parallel_cancellation_propagates_to_every_running_task` | T/C/H; external-resource conflicts need explicit ordering |
| Bounded foreach and matrix | compiler static expansion; runtime aggregate | `cargo test -p agentctl-core foreach_and_matrix_expand_to_stable_children_and_aggregates`; `cargo test -p agentctl-runtime foreach_aggregates_partial_results_and_retries_only_failed_children` | `cargo test -p agentctl-core expansion_bounds_and_binding_collisions_are_rejected` | T/C/H; 256 children maximum |
| Typed conditions and routers | compiler destination guards; durable runtime decisions | `cargo test -p agentctl-runtime typed_router_persists_decisions_and_recovers_when_the_route_changes` | `cargo test -p agentctl-core router_rejects_ambiguous_cases_and_implicit_dependencies` | T/C/H; no arbitrary expressions |
| Bounded loops and child recovery | compiler iteration graph; runtime loop aggregate | `cargo test -p agentctl-runtime bounded_loop_records_iterations_and_supports_retry_repair_replay_and_cancellation` | `cargo test -p agentctl-runtime bounded_loop_handles_zero_iterations_and_fails_closed_at_its_bound` | T/C/H; 1–64 precompiled iterations |
| Typed reusable sub-workflows | compiler expansion/namespacing; pack workflows | `cargo test -p agentctl-runtime subworkflow_boundaries_support_typed_execution_retry_repair_and_replay` | `cargo test -p agentctl-core subworkflow_rejects_cycles_and_invalid_versions`; `cargo test -p agentctl-runtime subworkflow_children_own_artifacts_and_report_namespaced_failures` | T/C/H; caller policy/providers remain authoritative |
| Working and cross-run memory | `core/src/memory.rs`; runtime adapters; store retrieval | `cargo test -p agentctl-runtime semantic_memory_uses_typed_external_contracts_and_replays_offline` | `cargo test -p agentctl-core semantic_memory_bounds_and_promotion_writes_are_compiled`; runtime typed adapter failures | T/C; local_hash is lexical, not neural embeddings |
| CAS ingest, verify, export, GC and corruption | `store/src/artifact.rs`; runtime artifact manifests | `cargo test -p agentctl-store ingest_deduplicates_verifies_exports_and_detects_corruption`; `cargo xtask artifact-store-verify` | `cargo test -p agentctl-runtime repair_uses_cas_after_workspace_deletion_and_blocks_blob_corruption` | T/C; regular output files ≤16 MiB, CAS backup required |
| Versioned SQLite, encryption and migration | `store/src/lib.rs`, `encryption.rs`; runtime legacy upgrade | `cargo xtask migration-verify`; `cargo test -p agentctl-runtime legacy_analysis_upgrade_imports_artifacts_and_enables_selective_repair` | `cargo test -p agentctl-runtime legacy_analysis_reports_the_earliest_safe_boundary_when_proof_is_missing`; encryption tamper/wrong-key tests in store suite | T/C; selected fields only, plaintext before explicit enable |
| Lock contention and local ownership | store WAL/busy timeout and optimistic transitions | `cargo test -p agentctl-store` | Same suite exercises contention, transitions and budget reservation races; packaged acceptance lock scenario | T/C; no distributed lease service |
| At-most-once effect uncertainty and reconciliation | `core/src/effect.rs`; runtime effect preparation; store reconciliations | `cargo test -p agentctl-runtime applied_reconciliation_supplies_a_validated_result_to_resume` | `cargo test -p agentctl-runtime repair_blocks_uncertain_mutation_until_not_applied_reconciliation`; `repair_rejects_unresolved_effect_on_otherwise_reusable_task` | T/C; no exactly-once remote mutation claim |
| Keyless effect-free replay and fresh fork | runtime `replay`/`fork`; CLI replay has no registry | `cargo test -p agentctl-runtime recorded_replay_never_calls_provider_or_tool_executor`; `recorded_replay_never_calls_provider_and_fork_does` | `cargo test -p agentctl-runtime replay_rejects_nonterminal_source_without_creating_partial_state` | T/C/H; encryption key may still be needed for encrypted history |
| Terminal retry | runtime retry planning/materialization; CLI retry | `cargo test -p agentctl-runtime terminal_failed_only_retry_reuses_success_and_replays_offline` | `cargo test -p agentctl-runtime retry_planning_enforces_identity_roots_acknowledgement_and_reconciliation` | T/C/H; identical workflow digest required |
| Selective repair and unchanged-boundary reuse | runtime plan_repair/repair and task fingerprints | `cargo test -p agentctl-runtime repair_reuses_tool_using_upstream_without_provider_or_tool_dispatch` | `cargo test -p agentctl-runtime repair_blocks_missing_state_delta_and_tampered_reused_output_digest`; `repair_plan_uses_minimal_branch_closure_and_blocks_changed_upstream` | T/C/H; instruction capture race remains G at checkpoint |
| Explicit best-effort compensation | runtime compensation workflow and linked reconciliation | `cargo test -p agentctl-runtime compensation_runs_in_reverse_order_reconciles_and_replays` | `cargo test -p agentctl-runtime uncertain_compensation_blocks_repeat_until_reconciled`; `compensation_continues_after_failure_and_retries_only_remaining_effects` | T/C; inverse execution is not transactional rollback |
| Pack locking, integrity, trust and transitive resolution | `core/src/pack.rs`; `cli/src/packs.rs` | `cargo test -p agentctl-cli resolves_transitive_local_graph_deterministically_and_offline`; `pinned_git_cache_is_reusable_offline` | `cargo test -p agentctl-cli locked_mode_rejects_tamper_and_unsigned_process_by_default`; `verifies_sigstore_bundle_identity_and_rejects_tamper`; `immutable_archive_is_bounded_cached_and_rejects_links` | T/C; remote instruction/variable assets require origin/integrity work |
| Process extension contract | compiler negotiation, runtime handshake/invocation | `cargo test -p agentctl-runtime process_extension_negotiates_contract_and_replays_without_execution` | `cargo test -p agentctl-runtime process_extension_refuses_mismatched_handshake_before_invocation`; `process_extension_bounds_output_timeout_crash_and_cancellation` | T/C; trusted executable contract, not native ABI or sandbox |
| MCP version/schema checks and bounded reconnect | `protocols/src/lib.rs` | `cargo test -p agentctl-protocols mcp_negotiates_session_lists_and_calls_tools`; `cargo xtask protocol-resilience` | `cargo test -p agentctl-protocols mcp_rejects_version_mismatch`; resilience suite schema-change/uncertain-dispatch fixtures | T/C; no live peer evidence |
| A2A identity-based continuation and artifacts | `protocols/src/lib.rs`; runtime continuation materialization | `cargo test -p agentctl-runtime continued_a2a_boundary_is_materialized_without_resubmission`; `cargo xtask protocol-resilience` | Protocol suite rejects ambiguous initial sends without task ID and invalid artifacts | T/C; no live peer evidence |
| Trace/audit/run/effect correlation and secret-safe output | `observability/src/lib.rs`; runtime trace bridge; CLI envelopes | `cargo test -p agentctl-observability`; `cargo test -p agentctl-cli machine_envelope_is_versioned` | `cargo test -p agentctl-core redacts_keys_and_embedded_secret_values`; streaming/process redaction regressions above | T/C; new source snapshots/origin diagnostics need redaction checks |
| Stable CLI exits and packaged clean-directory execution | CLI envelope/exit mapping; xtask acceptance/package | `cargo xtask acceptance`; `cargo xtask package` | `cargo test -p agentctl-cli malformed_command_is_usage_exit_two`; acceptance codes 2/3/4/5/6/130 | T/C; previous interrupted verify is not a pass |
| Supply chain, supported targets and OCI | `.github/workflows`; xtask metadata/security/package | `cargo xtask verify`; `cargo xtask secret-scan`; hosted PR workflows | Secret fixture, full-tree/history scan, dependency/image scans and package digest checks | T/C/H; exact final-source hosted runs still required |
| Discoverable examples and current-format inventory | `xtask/src/main.rs:496,605`; `docs/execution/EXAMPLE_VERIFICATION_MATRIX.md` | `cargo xtask examples-verify` | Matrix exact-inventory and negative capability cases | S/C/G; existing gate executes a subset, not all 20 requested DevOps workflows |
| OpenAI live semantic/tool/composite/repair validation | `xtask/src/acceptance.rs:3607–4254` | `cargo xtask acceptance-live-openai`; `cargo xtask examples-verify-live-openai` | `cargo xtask resource-budget-live-openai` expected durable request-2 denial | H/G; commands overlap, containers are included, seven-path inventory fixed, no shared suite cap at checkpoint |
| Documentation content/build and public boundary | canonical `docs/`; separate site repository | `cargo xtask docs-verify`; site `AGENTCTL_REPO` verification | Broken links, generated freshness and site/browser tests | S/H/G; final cross-repository SHA and boilerplate-removal proof required |

## Gate coverage and overlap

Inspect current `xtask/src/main.rs` and `xtask/src/acceptance.rs` before scheduling gates. `cargo xtask verify` covers formatting, Clippy, production build/tests, deterministic coverage, generated artifacts, example inventory/execution, documentation, supply-chain/security, package installation and production boundaries. `acceptance` packages the real binary and runs 46 scenarios; `completeness` packages and runs the durable composite. `examples-verify` includes static validation and selected execution; the twenty-case DevOps runner is separately discoverable and integrated into inventory/verification. Checked YAML alone never establishes execution.

`artifact-store-verify`, `migration-verify` and `protocol-resilience` wrap test filters already included in the full workspace suite; separate log names are not required to invent extra coverage or justify duplicate runs. Live gates intentionally include OCI execution. The OpenAI inventory now contains seven legacy paths and four distinct DevOps variants, parsed as YAML rather than detected through fragile text matching. Focused composite/container continuations retain explicit partial inventory. Ordinary CI remains credential-free; paid gates require the one existing aggregate allowance and explicit model configuration.
